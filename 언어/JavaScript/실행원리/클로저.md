# 클로저
## 정의
- 함수가 외부 변수(스코프)를 기억해서 함수 실행이 끝난 후에도 그 변수에 접근할 수 있는 것

## 예제
```js
function outer() {
  let count = 0;

  return function () {
    count++;
    console.log(count);
  };
}

const fn = outer();

fn(); // 1
fn(); // 2
```

- 반환된 함수가 외부 변수인 count에 참조
- 참조가 남아있어서 메모리에서 삭제되지 않음

