# 동기
- 코드가 순차적으로 실행됨.
- 하나의 작업이 끝나야 다음 작업이 실행되므로 오래 걸리는 작업이 있으면 전체 흐름이 멈춤.
- 예: 파일 읽기, 네트워크 요청을 동기적으로 처리하면 브라우저가 멈춘 것처럼 보임.
# 비동기
- 특정 작업을 기다리지 않고 다음 코드 실행
- 특정 작업을 **백그라운드에서 처리**하고 완료되면 콜백/Promise/async-await으로 결과를 전달.
- 메인 스레드가 멈추지 않고 다른 작업을 계속 진행 가능.
