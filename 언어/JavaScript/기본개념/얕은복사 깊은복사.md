# 얕은복사(Shallow Copy)

## 정의

- 객체의 최상위 프로퍼티만 새로운 값으로 복사 중첩된 객체는 참조값(주소)만 복사

## 특징

- 원본과 복사본이 중첩 객체를 공유 → 복사본 수정 시 원본도 영향을 받음

# 깊은복사(Deep Copy)

## 정의

- 객체의 모든 레벨을 새로운 메모리에 복사 → 원본과 복사본 완전히 독립
## 특징

- 복사본 수정 시 원본에 영향 없음