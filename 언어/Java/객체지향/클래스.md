# 클래스

## 정의

- 객체를 만들기 위한 설계도
- 필드와 메소드를 정의하는 사용자 정의 타입

```java
class User {
    String name; // 필드(속성)

    void say() { // 메소드
        System.out.println("hello");
    }
}
```

- 변수 + 메소드를 정의
- 객체를 생성하기 위한 틀

## 특징

- 클래스 자체는 메모리에 객체로 존재 X
- 객체를 생성ㅎ