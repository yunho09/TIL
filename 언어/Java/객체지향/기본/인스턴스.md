# 인스턴스

## 정의

- 클래스를 기반으로 생성된 실제 메모리에 존재하는 객체

## 생성 방법

```java
User user1 = new User();
```

- 메모리에 객체 생성

## 특징

- 각각 독립적인 데이터 보유
- 같은 클래스로 여러 인스턴스 생성 가능

```java
User user1 = new User();
User user2 = new User();
```