# 메소드

## 정의

- 클래스 내부의 정의 된 함수

## 예제

```java
class User {
    String name;

    void say() { // 메소드
        System.out.println(name);
    }
}

User user1 = new User();
user1.say();
```

- 객체를 통해 호출
- 필드 활용 가능
- 재사용 가능

## 메소드 종류

- 인스턴스 메소드 → 객체 필요
- static 메소드 → 객체 없이 호출