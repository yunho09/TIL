# Wiki Index

Claude가 관리하는 페이지 카탈로그. 페이지당 한 줄 요약. 새 페이지가 생기거나 요약이 바뀌면 여기도 함께 갱신한다.
섹션 제목 = `wiki/` 아래 실제 폴더 경로.

## 페이지

### 메타 (위키/vault 운영)
- [[위키-자동-캡처]] — Karpathy LLM Wiki 모델 채택 이유, SessionEnd 훅 기반 자동 ingest+push 파이프라인, 스킵 조건, 적용 범위/한계, Notion 자동 연동 미채택 이유

### 환경/GNOME
- [[GNOME-오버뷰-창-미리보기-사라짐]] — 3손가락 스와이프 후 가끔 창 미리보기만 비는 문제, `_gestureEnd` 예외 가설은 진단 로그로 반증됨(원인 미확정), 자동 감지+덤프 익스텐션 설치해 다음 재현 대기 중, GNOME 확장은 코드 변경 시 핫로드 안 되고 로그아웃/로그인 필요(`ReloadExtension` D-Bus deprecated)
- [[GNOME-Wayland-wl-clipboard-포커스-토스트]] — Mutter 50에 data-control 프로토콜 부재→wl-clipboard 폴백 경로→`focus-new-windows=strict`가 겹쳐 뜨는 정체불명 토스트의 원인 체인과 `smart` 복구법
- [[Vitals-확장-상단바-시스템-모니터]] — 자체 스키마를 쓰는 확장은 `gsettings --schemadir`로 확장 설치 경로의 schemas를 직접 지정해야 하는 이유(Vitals `hot-sensors`/`position-in-panel` 예시), GNOME 50에서 D-Bus 스크린샷이 `AccessDenied`로 막혀 있음

### 환경/리눅스-데스크톱
- [[PC-중복-설치-정리-Homebrew-도입]] — Linuxbrew 도입 후 apt/수동 설치와 중복되던 tmux·mise를 brew로 통일(브랜드 tmux client가 기존 apt tmux server 세션에 그대로 재접속돼 끊김 없이 교체 가능했다는 사실 포함), 로그인 셸(zsh)·essential 패키지(bash)는 위험해서 보류, ghostty deb/snap 중복(brew ghostty는 macOS 전용이라 대체 불가), wezterm·postman/notion snap·`.nvm` 등 앱 교체로 죽어있던 설치본 판별법(libfuse2 없는 AppImage, 실행 경로 교체 시점 대조), `.desktop` 오버라이드가 중복이 아니라 XDG 우선순위로 snap을 가리는 의도된 구조라는 것, docker-ce/Docker Desktop 이중설치·codex 설치 깨짐은 미해결로 남김
- [[GPU-유휴클럭-램프업-지연-프레임드랍]] — 창 드래그·오버뷰 애니메이션만 버벅이고 계속 움직이면 부드러워지는 증상의 원인은 GPU 유휴 클럭 400MHz 고정+최대클럭 램프업 ~1.5초인데 애니메이션은 0.2~0.4초라 못 따라가는 것, 컴포지터/패널 60Hz 하락·Discord `--disable-gpu`라는 오진단 2건은 실측으로 반증, wayland presentation-time 프로토콜·120Hz vs 60Hz 시각 비교자·act_freq 샘플링 진단 기법, `min_freq` sysfs로 임시 해결(재부팅시 원복)과 배터리 트레이드오프, XWayland 프레임 페이싱은 별개 미해결 이슈로 남음
- [[절전-복귀-지연-원인과-zram-도입]] — 스왑 고갈+복귀 직후 systemd 타이머 폭주가 겹쳐 절전 복귀가 9초 넘게 걸리던 원인, zram 압축 스왑 도입(설치 시점 기본값 함정 포함)과 타이머 완화 조치. zram 도입 후에도 재발 — 진짜 급소는 gnome-shell 자신이 zram으로 내보낸 페이지 재적재, `CPUWeight`가 형제 슬라이스끼리만 경쟁한다는 함정, MemoryMin/background.slice/NVMe 스케줄러/fstrim 시간 이동 등 6가지 추가 조치. **최종 확인된 진짜 원인은 복귀가 아니라 전원 버튼 절전 진입 지연** — gnome-shell의 화면 시간(Screen Time) 기록 기능이 sleep 억제자를 놓지 않아 logind가 `InhibitDelayMaxSec` 5초를 다 채운 뒤에야 절전에 들어간 것, `Lid opened`만 측정하던 도구의 사각지대
- [[Ptyxis-터치패드-스크롤-속도-패치]] — GTK4 `-Bsymbolic`으로 LD_PRELOAD 차단, `enable-fallback-scrolling=false`로 VTE 패치가 스크롤백에 안 먹히던 원인, `DBusActivatable=true`가 PATH 래퍼를 우회하는 함정, 스크롤백/앱 배율 분리 + bypass 패치로 해결
- [[터치패드-전역-스크롤-속도-libinput-quirks]] — Chrome 등 모든 앱의 터치패드 스크롤이 느렸던 원인은 `/etc/libinput/local-overrides.quirks`의 `AttrResolutionHint=240x240`(mm당 해상도를 속여 스크롤량 축소), 실제 네이티브 해상도 120/mm 역산, 같은 값이 커서 속도·3손가락 제스처 임계값에도 걸리는 부작용, `pad-scroll-speed` 스크립트로 무로그아웃 적용, 터치패드/마우스휠(scroll-boost)/터미널(Ptyxis) 3개 스크롤 경로가 서로 독립적이라는 정리
- [[Orca-IDE-리눅스-설치]] — AppImage type 2가 Ubuntu 26.04에서 안 열리는 이유(libfuse2 부재)와 압축 해제 설치법, `orca`↔GNOME 화면낭독기 이름 충돌, 자동 업데이트 불가 등 한계
- [[리눅스-메모리-점유-앱별-진단-PSS]] — RSS 합산은 공유 메모리 중복 계산으로 부풀려짐, PSS로 앱별 합산해야 정확, zram/Shmem/slab 등 앱 외 요소까지 더해야 총량이 맞는 이유, Electron 앱은 하나 끄면 런타임째 통째로 회수되는 정리 우선순위
- [[Figma-데스크톱-앱-메모리-중복]] — 비공식 Electron 래퍼(`figma-linux-next`)가 Chromium 런타임을 중복으로 띄우는 원인(브라우저판 대안), 열어둔 탭은 자동 해제 안 됨, `settings.json`을 열린 탭 목록으로 오인하면 안 되는 이유, 탭↔렌더러 PID 매칭 불가로 안전한 정리법은 앱 내 직접 조작뿐
- [[Figma-터치패드-핀치줌-속도-패치]] — `app.asar` 전개+preload 주입으로 핀치줌 배속(`figma-zoom-speed`, CDP로 검증), "확대가 안 되고 그냥 움직여짐" 증상은 핀치 종료 ~190ms 뒤 남은 움직임이 libinput에 새 스크롤 제스처로 재분류되는 것(+`AttrResolutionHint` 스크롤 2배 부작용 중첩)이 원인, 핀치 꼬리 300ms 가드로 대응(사용자 확인 대기 중, 미확정)
- [[Electron-워크스페이스-고정-창-포커스탈취]] — `setVisibleOnAllWorkspaces(true)`는 리눅스에서 `_NET_WM_STATE_STICKY`(X11/XWayland 전용, 네이티브 Wayland는 무효)로 동작, override-redirect 창(입력용 히트 창 등)은 원래도 전체 워크스페이스에 있다는 사실, macOS는 show마다 재적용 필요하지만 리눅스는 생성 시 1회로 유지되는 차이, 창을 전체 워크스페이스에 고정하면 워크스페이스 전환 시 키보드 포커스가 그 창으로 넘어가는 미해결 부작용

### 라즈베리파이/GPIO
- [[GPIO-기초]] — GPIO가 무엇인지(3.3V 출력/입력), 켜짐·꺼짐뿐인 핀으로 PWM이 밝기를 만드는 원리, BCM 번호와 물리 핀 자리 번호가 다른 두 체계, 핀은 한 프로세스만 점유한다는 성질, 40핀 헤더에서 1번 핀 찾는 법과 3.3V·5V·GND 위치
- [[gpiozero-GPIO-배선-확인]] — gpiozero는 LED가 없어도 에러를 안 내므로 터미널 출력이 무의미, 핀 스윕으로 실제 배선 핀 찾기, BCM 번호와 물리 핀 번호 혼동, `GPIO busy`(같은 핀을 쓰는 프로그램 중복 실행)와 잔여 프로세스 정리, 동시 실행은 가능하고 배타적인 건 핀·포트 단위라는 점과 핀 소유자-클라이언트 구조, 센서 같은 입력 장치는 풀다운 입력으로 읽어서 찾기, 선이 부딪히면 GND 공유·신호 핀 이동
- [[gpiozero-PWMLED-밝기-제어]] — 켜고 끄는 LED와 달리 PWMLED는 value 0.0~1.0으로 조광, 슬라이더 0~255를 나누어 매핑할 때 STEPS 불일치 함정, PWM이 핀을 계속 점유하는 성질
- [[DHT11-온습도-센서]] — Pi 4·Debian 13·Python 3.13에서 동작한 조합(adafruit-circuitpython-dht를 venv에 pip, `use_pulseio=False`; 기본값은 `Unable to set line`으로 실패), `sensor not found`가 뜻하는 것, 3.3V 핀 옆 5V 핀 주의, 풀다운 입력 읽기로 DATA가 꽂힌 핀 찾기(HIGH 핀이 두 개면 배선 오류 신호)

### 라즈베리파이/PyQt5-GUI
- [[라즈베리파이-GUI-개발-워크플로]] — PC에서 Designer+pyuic5로 만들고 파이에서 실행하는 전체 흐름, 어느 기계/어느 터미널에서 무엇을 하는지 구분표, 가짜 gpiozero를 끼워 PC에서 로직을 미리 검증하는 법, 자주 막히는 지점 모음, 며칠 뒤 이어서 할 때 체크리스트(IP 재확인·최신 파일 재전송)
- [[라즈베리파이-PyQt5-설치-ARM64]] — pip PyQt5가 ARM64에서 소스 빌드로 빠지는 문제, apt `python3-pyqt5` + venv `--system-site-packages` 조합, Pi 5용 LGPIOFactory 명시가 Pi 4에서는 불필요한 이유, 한글이 □□로 깨질 때 `fonts-nanum`
- [[라즈베리파이-GUI-SSH-VNC-실행]] — GUI는 SSH 터미널(=DISPLAY 없음)에서 못 뜬다, 코드가 import 시점에 `QT_QPA_PLATFORM`을 덮어써 셸 환경변수가 무시되는 함정, Debian 13의 wayvnc 활성화와 세션 없으면 붙을 게 없는 구조, PC 터미널과 파이 터미널 구분(scp는 PC에서, PowerShell은 `&&` 미지원), SSH/SCP/VNC 역할 구분 — VNC는 저장소가 아니라 화면이고 파일은 파이 디스크에만 있다, 며칠 뒤 IP가 바뀌는 문제와 VNC 뷰어에 남은 옛 주소, wayvnc 재부팅 후 자동 시작
- [[Qt-Designer-PyQt5-연결]] — objectName이 디자인↔코드의 유일한 연결고리, 폼 objectName을 잘못 바꾸면 `Ui_` 클래스명이 바뀌어 코드가 어긋남, 레이아웃 단축키는 Ctrl+1/2/5, Qt5/Qt6 Designer의 .ui는 호환되지 않음, 반복문 시그널 연결 시 람다 늦은 바인딩, 표시와 적용 분리 패턴, pyuic5가 XML을 파이썬 코드로 번역하는 방식과 생성 파일을 직접 고치면 안 되는 이유
- [[가짜-모듈로-하드웨어-없이-테스트]] — `sys.modules`에 껍데기 모듈을 미리 등록해 하드웨어 라이브러리 import를 가로채는 기법, offscreen 렌더로 GUI 버튼까지 코드로 눌러 검증, 이 방식으로 걸러지는 것과 걸러지지 않는 것, offscreen에서 글자가 안 찍힐 때 `show()` 없이 `grab()`, 시간에 따라 변하는 가짜 센서로 그래프 화면 캡처
- [[Qt-스타일시트-QSS]] — 폼 `styleSheet`에 QSS를 넣어 `.ui`에 저장, QLabel이 QFrame을 상속하므로 ID 선택자로 범위 한정, 세로 슬라이더는 `add-page`가 채워지는 아래쪽, 네온 발광은 QSS가 아닌 `QGraphicsDropShadowEffect`로, 다크 네온 팔레트
- [[Qt-Designer-위젯-승격]] — 직접 만든 위젯을 Designer에 넣는 승격(Promote), `.ui`의 `customwidgets`와 pyuic5가 붙이는 import(모듈 파일을 같이 옮겨야 함), QPainter로 그린 실시간 라인 차트(deque 보관·y축 최소 폭·여러 겹 선으로 발광), 레이아웃 `stretch`

### 언어/Java/Spring
- [[빈과-DI]] — 인터페이스+구현체+생성자 주입 패턴, 다중 구현체 주입(@Primary/@Qualifier/List<T>), 싱글톤 규칙, 실무 사용 빈도
- [[스프링-컨테이너]] — 컨테이너=Map 비유, 빈은 메모리에만 존재, 조립 순서, @SpringBootApplication 구성, 컴포넌트 스캔 기준점과 src 폴더 구조
- [[스프링-CRUD-계층구조]] — Controller/Service/Repository 계층별 역할, 요청 값 추출 3종(@PathVariable/@RequestParam/@RequestBody), URL+메서드 매핑, Postman/curl 테스트 방법
- [[JPA-엔티티와-리포지토리]] — @Entity 규칙(빈 생성자, Long id, setter 대신 update()), JpaRepository가 빈 인터페이스로 동작하는 원리와 쿼리 메서드
- [[MySQL-연동]] — datasource 설정, ddl-auto 선택지, Docker MySQL 컨테이너 명령어 모음

### 언어/JavaScript
- [[Nullish-Coalescing-빈문자열-함정]] — `??`는 `null`/`undefined`만 폴백하고 `""`/`0`/`false`는 안 걸러서, 파싱 결과가 정상적으로 빈 문자열이 되는 경우 의도한 fallback이 발동 안 함(`||`와의 차이), 같은 헬퍼가 앱마다 `??`/`||`로 갈린 divergence 실사례
- [[requestAnimationFrame-래치-가드-버그]] — rAF 스로틀 가드를 미리 busy로 두고 콜백이 안 오면(백그라운드 탭 로드 등) 영원히 잠겨 이후 이벤트가 전부 무시되는 패턴, `visibilitychange`/`pageshow` 복구 + 워치독 타이머 해결
- [[React-Router-useBlocker-데이터라우터-필요]] — 작성 중 이탈 방지는 앱 내부 이동은 `useBlocker`, 탭 닫기·새로고침은 `useBeforeUnload`로 나눠 막고, `useBlocker`는 data router(`createBrowserRouter`)에서만 동작해 JSX `<Routes>`면 라우트 정의 전환이 선행돼야 한다는 것
- [[React-useEffect-useLayoutEffect-폼제출-타이밍-버그]] — 첨부 제거 직후 바로 저장하면 옛 목록이 전송되는 버그의 원인은 파생 상태 동기화가 페인트 이후 비동기 실행되는 `useEffect`였기 때문, 페인트 전 동기 실행되는 `useLayoutEffect`로 교체해 해결, 빠른 연속 입력-제출 폼에서 파생 상태 동기화에 `useEffect`를 쓰면 레이스 컨디션이 생길 수 있다는 일반 패턴

### 프론트엔드/CSS
- [[CSS-Container-shrink-to-fit]] — `max-width`는 상한일 뿐 폭을 확보 못 함, `main` shrink-to-fit 원인과 해결, 전체 배경색 우회법. 후속 함정: shrink-to-fit을 고쳐 `main`에 `width:100%`를 주면 교차축 `auto` 마진이 무력화돼 중앙 정렬이 깨짐 → 폭을 제한하는 자식(`Container`)에 `margin-inline: auto`를 둬서 해결
- [[CSS-fieldset-legend-flex-패딩-무시]] — `display:flex` fieldset에서 `<legend>`가 padding 무시하고 최상단에 붙는 원인과 float+clear 해결법, 고정 px 그리드→`fr` 반응형 전환, 한글 `word-break: keep-all`
- [[테이블-오버플로우-hidden-드롭다운-메뉴-잘림]] — 표 컨테이너의 `overflow:hidden`이 행의 케밥/드롭다운 메뉴까지 잘라 마지막 행에서 메뉴가 통째로 안 보이는 원인, `overflow` 제거 후 `border-radius`를 헤더에 직접 부여하는 해결법
- [[backdrop-filter-blur-애니메이션-배경-스크롤-끊김]] — 블러 레이어 아래 배경이 매 프레임 바뀌면(줌/크로스페이드 사진, scale 애니메이션) 재래스터화로 스크롤이 끊기는 원인, 캔버스를 동기 실행으로 먼저 배제하는 진단 순서, DPR 캡·블러 제거+배경 불투명도 보상·자가 저하 해결법
- [[표-내부-height-지정-행-높이-분배]] — 바깥 프레임뿐 아니라 표 자체(`.tbl`)에도 height를 주면 남는 높이가 행에 분배돼 행이 적을 때 비정상적으로 커지는 버그, `vertical-align:middle`과 겹쳐 글자가 표 한가운데로 밀려 보이는 증상, 표의 height만 제거하는 해결
- [[큰-텍스트-overflow-hidden-하강선-잘림]] — 대형 디스플레이 텍스트를 등장 연출용 `overflow:hidden` 래퍼에 담을 때, 래퍼 높이를 `font-size` 비율로만 계산하면 `g`/`y`/`p` 하강선이 잘리는 원인과 래퍼 높이를 글자 크기에 맞추고 등장 연출은 `transform`으로만 구현하는 해결

### 프론트엔드/스크롤-인터랙션
- [[토스식-핀고정-스크롤-장면]] — toss.im·kakaocorp·10to100 DOM 계측 결과 "3D처럼 보이는" 스크롤 연출의 정체는 `perspective`/`matrix3d`가 아니라 긴 부모+`position:sticky` 자식+스크롤 진행도로 transform·opacity만 조종하는 핀 고정 안무라는 것, sticky가 씹히는 overflow 조상 조건, 효과보다 스크롤 분량(뷰포트 수) 자체가 체감을 좌우한다는 점, 점진적 향상 안전장치
- [[무한-마퀴-드래그-클리핑-컨테이너-대신-트랙에-적용]] — 무한 티커에 스크롤 반응 드래그를 걸 때 바깥 `overflow:hidden` 클리핑 컨테이너에 transform을 걸면 가장자리에 틈이 생기는 이유, 안쪽 트랙에만 적용해야 하는 해결
- [[스크롤-오브젝트-연출-패턴-모음]] — 카드 딜(fan-out)·클립패스 리빌·타임라인 드로잉·플로우 라인·도어 오픈+SVG 도장·conic-gradient 회전 테두리 글로우·배경 대형 마퀴 고스트 텍스트 등 재사용 가능한 스크롤 트리거 연출 부품, 상태 클래스 조합에서 CSS 특이도가 밀리는 함정, 대형 JS 엔진 출력 한도 우회를 위한 분할 배포, 배포 후 curl 바이트 대조 검증, 로컬 프로토타이핑 하네스(http.server·뷰포트 에뮬레이션·디지털 줌·오래된 스크린샷 우회) (2026-09-18)

### 프론트엔드/빌드도구
- [[Vite-빌드타임-환경변수-인라인]] — `VITE_*`가 런타임이 아니라 빌드 시점에 번들에 문자열로 치환됨, 그래서 비밀값이 될 수 없고 값 변경 시 재빌드 필요
- [[Vite-모노레포-워크스페이스-패키지-dev서버-캐시]] — 앱 밖 `packages/*` 워크스페이스 패키지 수정이 HMR에 안 걸려 dev 서버가 옛 변환 캐시를 계속 서빙하는 문제, `.vite` 캐시 삭제+`--force` 재시작으로 해결
- [[Vite-공용설정-publicDir-모노레포-경로]] — 공용 `vite.config.common.ts`의 `publicDir`을 `import.meta.dirname` 기준으로 계산하면 레포 루트 `public/`로 고정돼 앱별 `apps/<app>/public/`이 죽은 폴더가 되는 함정

### 프론트엔드/배포
- [[Cloudflare-Workers-SPA-fallback-404]] — SPA를 Cloudflare Workers로 배포했을 때 `/`는 되지만 라우터 하위 경로(`/login` 등) 새로고침·직접 접속에서만 404가 나는 원인(프론트 코드 문제 아님, 배포 플랫폼의 SPA fallback 미설정)과 `wrangler.jsonc`의 `not_found_handling: "single-page-application"` 해결법, Pages인 경우 `_redirects` 대안, curl 헤더로 Workers/Pages 판별하는 법. **(2026-09-17 추가)** `wrangler.jsonc` 자체가 없는 브랜치는 Workers Builds 체크가 즉시 실패할 수 있고, 체크 로그의 시작·종료 타임스탬프가 같은 초면 빌드가 돌기 전에 떨어진 것이라는 진단법

### 프론트엔드/디자인-연동
- [[Figma-Dev-Mode-MCP]] — 데스크톱 앱에 파일이 열려 있어야 동작, view-only 파일에서 씬 그래프 API 실패·스크린샷 폴백과 픽셀 계측 대안. 에셋 export 함정 3가지: `get_design_context` 이미지가 전 픽셀 투명일 수 있음, `download_assets`의 `export`는 부모 프레임에 잘린 합성 렌더일 수 있음, 원본은 `rawImages`로 받아야 함
- [[Figma-MCP-팀별-호출-한도]] — `claude.ai Figma` 호출 한도는 계정이 아니라 파일 소유 팀의 플랜에 걸림, `whoami`는 항상 성공, 페이지 루트 전체 덤프가 한도를 급격히 소모, 해결책 두 가지(자기 팀으로 Duplicate=일회성/소유자에게 팀 이전 요청=계속 쓸 파일)

### 프론트엔드/API-인증
- [[401을-세션만료로-오인한-로그인-루프]] — 백엔드가 권한 부족도 403 대신 401로 응답하고 프론트가 401을 무조건 세션 만료로 처리할 때 로그인 직후 화면이 스스로 로그아웃을 유발하는 원인 체인과 해결(토큰 유효성 우선 확인). Spring Security에서 흔한 원인은 `accessDeniedHandler` 미등록으로 `AccessDeniedException`이 401 entry point로 새는 것. 관련 함정: 인터셉터가 403에도 재발급 후 재시도하면 생성(POST)류 요청에서 중복 등록 위험이 생긴다는 것과, 이 판단이 리팩터링 중 회귀하기 쉽다는 점. **(2026-09-17 추가)** 반대로 403 처리를 아예 없애면 토큰 쿠키 자체가 사라져 헤더 없이 나가는 진짜 만료 요청(403으로 오는 케이스)까지 놓쳐 로그인이 조용히 풀리는 회귀가 생김 — 상태 코드 하나가 아니라 "그 코드가 나오는 원인이 몇 가지인지"로 분기해야 한다는 정리. **(2026-09-17 밤 추가)** reissue 엔드포인트만 빼고 인증 실패 전체가 403으로 오는 서버 사례(ToyVillage-Admin-FE) — curl로 앱 없이 401/403 사용처를 구분하는 진단 팁, 원인이 백엔드에 있어도 "프론트에서 403도 재발급 대상으로 넓힌다"로 대응 방향을 정한 결정
- [[S3-Presigned-URL-업로드]] — presign 2단계 흐름, Authorization 헤더가 서명을 깨는 이유, S3 CORS 필요성, Playwright의 Blob 바디 목킹 한계
- [[도로명주소-검색-API-신청]] — business.juso.go.kr 신청 절차(검색 API vs 팝업 API, 개발/운영 승인키 흐름 차이), `confmKey`/JSONP 사용법, 승인키가 URL 단위로 묶이는 이유
- [[TanStack-Query-무효화-키-null-vs-undefined]] — 쿼리 키의 빈 파라미터를 `?? null`로 채우면 prefix 무효화가 안 먹는 이유(TanStack Query는 `undefined`만 와일드카드로 취급), `params`를 그대로 넘겨 `undefined`가 되게 하는 해결
- [[TanStack-Query-mutate-콜백-언마운트-스킵]] — `mutate()`에 넘긴 per-call 콜백은 컴포넌트 언마운트 시 실행 안 되고 await도 안 되어 `isPending`이 캐시 무효화 완료보다 먼저 풀리는 문제, 해결은 부수효과를 `useMutation` 훅 옵션으로 이동, 넓은 키 무효화가 곧 제거될 좁은 쿼리를 헛도는 함정도 포함
- [[응답-검증-적정선-없음-vs-조회실패-구분]] — (2026-09-20 추가: 완화한 삭제 성공 판정이 큰 커밋에서 되살아난 회귀, 삭제 후 화면 상태까지 검증) 응답 검증이 너무 엄격하면 서버 성공을 실패로 표시하고(공지 목록 전체 오류, 201+message만 성공 인정) 너무 느슨하면 네트워크·서버 오류가 "없음"으로 보이므로, 화면에 꼭 필요한 필드만 검증하고 404만 "없음"·나머지는 재시도 가능 오류로 분리
- [[TanStack-Query-placeholderData-이전탭-데이터-조작-가능]] — (2026-09-19 추가: 수정 폼이 placeholder를 초기값으로 쓰면 로딩 중 저장 시 서버 값이 기본값으로 덮어써지는 변형, `isPending` 동안 폼 미렌더로 해결) `placeholderData: (prev) => prev`로 탭 전환 중 이전 데이터를 보여주는 동안 행별 액션 버튼이 그 이전 데이터를 대상으로 활성 상태로 남는 함정, `isPlaceholderData` 가드로 해결
- [[크로스오리진-파일-다운로드-a-download-무시]] — 파일 주소가 페이지와 다른 도메인이면 `<a download>`가 무시돼 새 탭으로 열리거나 파일명이 key로 나오는 원인, CORS GET 허용(fetch+Blob) vs `Content-Disposition` 헤더 두 해결 경로, 비공개 버킷은 presigned URL 필요. fetch+Blob 구현 시 공용 인증 axios 재사용 금지([[공용-인증-axios-외부도메인-토큰유출]] 참고)와 Safari용 Blob URL 지연 해제 추가
- [[공용-인증-axios-외부도메인-토큰유출]] — 인증 인터셉터가 URL의 도메인을 안 가리고 모든 요청에 토큰을 붙이는 구조에서, 절대 URL로 외부 CDN을 호출하면 토큰이 새고 그 서버의 403이 세션 만료로 오인돼 로그아웃까지 유발할 수 있는 원인과 `fetch` 우회 해법
- [[에러-응답-CORS-헤더-누락-상태코드-차단]] — 502 등 에러 응답에 `Access-Control-Allow-Origin`이 빠지면 브라우저가 응답을 통째로 차단해 프론트의 상태코드별 에러 매핑이 무력화되는 원인, `Server` 헤더로 프록시/오리진 구분·무인증 호출로 "이 요청만 실패"인지 판별하는 진단 순서, 백엔드/인프라에서만 고칠 수 있다는 결론. **(2026-09-17 추가)** preflight(OPTIONS) 상태코드로 preflight 실패와 본요청 CORS 헤더 누락을 구분하는 법, Cloudflare 502가 CORS 에러로 오인되는 실사례(`Server: cloudflare`)
- [[Vite-dev서버-프록시로-오리진고정-CORS-우회]] — 스테이징이 특정 오리진(예 `localhost:5173`)만 허용하고 그 포트가 이미 다른 프로젝트로 점유돼 있을 때, `server.proxy`로 브라우저→dev서버(같은 오리진)→백엔드(서버 대 서버, CORS 미적용) 경로를 만들어 사람이 직접 브라우저로 확인해야 하는 상황을 우회하는 기법과 적용 시 임시 설정 분리 요령

### 프론트엔드/테스트
- [[테스트-픽스처-고정날짜-현재월-필터-노후화]] — "이번 달"만 렌더하는 화면에 고정 과거 날짜 픽스처를 쓰면 작성 당시엔 통과하다 달이 바뀌면서 자동으로 실패하는 시간 의존 테스트 패턴, 판별법과 상대 날짜 전환 해결책
- [[배럴-Import-함정]] — 엔티티 배럴(`index.ts`)이 상수 하나 때문에 재수출 전체를 실행시켜 axios CJS 체인이 Node/Playwright 로더에서 링크 실패하는 원인, `Total: 0 tests in 0 files`로 조용히 수집조차 안 되는 게 가장 위험한 이유, 하위 모듈 직접 import로 우회, 집계 스크립트가 `"Total:"` 문자열만 보거나 `tail -1`로 판정하면 이런 실패를 놓치는 부수 함정. 다른 메커니즘: 배럴 경유 순환 참조가 top-level `styled(X)` 평가 시점에 `undefined`를 주입해 크래시나는 사례
- [[네트워크-레벨-모킹]] — 함수 교체/네트워크 가로채기/진짜 서버 3단계 비교, MSW·Playwright `page.route`·Cypress·nock·WireMock 도구별 구현 방식, fulfill/continue/abort/fallback, 고정·대본형·상태형 응답 패턴, route 등록 순서(나중 등록이 먼저 매칭)·등록 시점·서비스워커 우회·패턴 정확도 함정, 계약 승인+해시 동결로 contract drift를 보완하는 구조. **(2026-09-17 추가)** `api.e2e.invalid` 같은 도달 불가 도메인을 안전장치로 잡아 유출 요청을 확실히 실패시키는 기법, 해시 동결의 목적(AI가 테스트를 고쳐 통과시키는 걸 방지)과 테스트가 import하는 공용 헬퍼는 해시에 안 잡혀 우회 구멍이 남는 한계. **(2026-09-17 밤 추가)** 같은 구멍이 산문 스펙 문서(`.spec.md`)에도 있음 — 모킹 방식을 바꿔도(예: localStorage→네트워크 레벨) 문서 설명은 해시 보호 밖이라 낡을 수 있고, 외부 리뷰(CodeRabbit)가 diff 밖 드리프트를 짚어준 사례
- [[스토리북-전용-Vitest-설정에-묻힌-유닛테스트]] — `storybookTest` 플러그인이 `include`를 스토리 파일로 고정해 `.test.tsx`가 수십 개 있어도 한 번도 안 도는 함정, `vitest.unit.config.ts` 분리 해결, jsdom vs 브라우저 모드 선택 기준(계산된 CSS 값 차이), `globals:true` 없으면 testing-library 자동 cleanup이 등록 안 돼 DOM이 누적되는 문제, Playwright 브라우저 빌드 버전 불일치·리눅스 배포판 미지원 시 캐시 심볼릭 링크 우회
- [[Playwright-toBeVisible-조상-클리핑-미탐지]] — `toBeVisible()`은 요소 자신만 검사하고 조상의 `overflow:hidden` 클리핑은 못 잡는 함정, hit-test(`elementFromPoint`)로 실제 렌더 가시성을 검증하는 대안
- [[Playwright-reuseExistingServer-실서버-유출]] — `reuseExistingServer`가 baseURL 포트의 기존 서버를 그대로 재사용해 mock 안 된 요청이 실제 staging으로 새는 함정(다른 프로젝트 점유·자기 dev 서버가 실서버를 향한 경우 둘 다), 격리 포트+도달 불가 API로 재실행해 기존 실패와 구분하는 판별법
- [[Playwright-mock-오리진-미검증]] — route 매처가 pathname만 보고 origin을 안 보면 요청이 엉뚱한 서버로 새도 테스트가 계속 통과하는 함정, 매처·기대값 양쪽에 origin 추가로 해결, `reuseExistingServer`로 `webServer.env`가 안 먹는 로컬 환경은 기대 오리진을 환경변수에서 읽어 대응
- [[Playwright-오토스크롤-화면기준-위치측정-오염]] — 클릭 전 Playwright가 대상을 뷰포트로 자동 스크롤해 위치 고정 검증을 화면(뷰포트) 기준 좌표로 하면 화면 버그 없이도 테스트가 실패하는 함정, 페이지(문서) 기준 좌표로 측정을 바꿔 해결

### 프로젝트/spring-practice
- [[프로젝트-현황]] — spring-practice 구조·API·DB 환경·알려진 허점 스냅샷 (2026-09-02)
- [[학습-진행상황]] — 스프링 로드맵 진행 상태, 겪은 에러들, 다음 단계 후보

### 프로젝트/JOBIS-FE-V2
- [[JOBIS-FE-V2/프로젝트-현황]] — 퍼블리싱·커밋 컨벤션(이슈 1=브랜치 1=PR 1 기본값), `packages/api` 공용 코드 결함(presign/더블슬래시 수정완료·단 브랜치별 재발 확인됨/401 이중의미/캐시 무효화 누락), `updateParams`↔`getParam` 키 표기 불일치로 필터 죽는 버그 클래스, 첨부파일 종류를 서버가 구분 못 하는 설계 한계, 파일 업로드 스펙 두 벌 충돌(미해결), 백엔드 API 확인사항(status enum, acceptances 취소 의미, 조회수, 배너), acceptances 전체 조회 API 부재 명세서로 재확인, 어드민 취업관리(`/student`) 화면 — API 연동·자동선택·체크박스 동작 전부 이번 브랜치에서 제외하고 순수 퍼블리싱만 유지하기로 한 결정과 복구 경로, 버그 제보 페이지 Figma 정합화로 디자인시스템 확장(`Input`/`TextArea` underline variant, `FileUpload` 스펙 변경이 어드민 공지 등록에도 영향) 및 학생 앱 라우터 `main` shrink-to-fit 전역 수정. **(2026-09-10 갱신)** `main width:100%` 회귀로 전 페이지 좌측 쏠림 → `Container margin-inline:auto`로 수정, 공지 첨부파일명 `??`/`||` divergence 수정, 공지 12개 페이지네이션은 스테이징 서버 미지원 확인돼 보류, 홈 화면 흰 화면 버그는 토큰 재발급 큐 데드락으로 추가 의심, **어드민 `Footer` 테마 크래시가 재현되며 09-09의 "`--force` 탓" 결론과 상충**(원인 미확정 상태로 재오픈). **(2026-09-11 갱신)** 커밋에 Claude attribution 라인 붙이지 않기로 결정(기존 이력 재작성 완료), 디자인시스템 `.test.tsx` 46개가 storybookTest `include` 고정으로 한 번도 안 돌고 있던 문제 발견·분리 해결(418건 통과), `Modal` 순환참조·`Icon` 강제 aria-label 접근성 부채(59곳 영향, 보류)·죽은 테스트 3건 정리, `createQueryHook` params-누락 URL 버그와 공지 캐시 무효화 `null` 버그 수정, 403 인터셉터 재시도 회귀 원복, 학생 앱 이미지 URL 접두사 미적용·버그제보 API 403(서버측, 미해결) 신규 이슈, PR #147이 스택 PR 7개(#148~154)로 재구성됨. **(2026-09-11 야간 갱신)** 버그 제보 403 원인이 서버측이 아니라 프론트 경로 오타(`/bug-reports`→`/bugs`)로 확정·수정, `GET /interviews/students`는 백엔드에 애초에 없는 API로 확정(PR #1147 closed, 백엔드 방향 결정 대기 중인 유일한 항목), 마이페이지 학과명 한글 표시(Figma 띄어쓰기 기준 확정, `COMMON`→"공통과정" 사용자 결정), 헤더 학생 이름 실연동과 알림 `isNew` 최초 렌더링 고정 버그 수정, 버그 제보 글자수 제한(20/400자, 백엔드 제한과 동일) 추가, `@playwright/test` 기반 로그인 e2e 스위트 신규 추가(staging CORS가 `5173`/`5174`/`3000`만 허용, `apps/student` 단독 `yarn dev`가 전역 Vite를 띄우는 함정), 스택이 #148~#158(11개)로 확장되고 팀 분담표 9항목 중 8항목 완료 확인. **(2026-09-17 갱신)** #146 스택 11개 브랜치 코드 리뷰 14개 항목 전부 처리·origin 반영 완료 — 리뷰가 지목한 커밋 4개 누락으로 스택 어긋남 재발견·순차 rebase로 해결([[Git-스택-PR-재정렬-force-with-lease]]), 403 처리를 401로 좁힌 이전 결정이 "쿠키 만료 시 헤더 없이 403" 케이스를 놓쳐 로그인이 조용히 풀리는 회귀를 만들어 "access_token 쿠키 없을 때만 재발급"으로 재조정([[401을-세션만료로-오인한-로그인-루프]]), 공지 폼 좌표를 "구분선에 맞추는 게 의도"로 잘못 역산했다가 뒤늦게 이슈 본문에서 찾은 Figma 시안과 대조해 정정(입력칸 실측은 input이 아니라 wrapper 박스 기준이어야 함), 버그 제보 제보내용 칸을 시안 부재(이슈#137 "디자인 없음" ↔ PR#154 "시안 반영" 기록 불일치) 상태로 1줄→5줄 확정, e2e 로그인 캐시 검증 시 실패 케이스가 섞이면 Playwright 워커 재시작으로 캐시가 초기화돼 오염된다는 것 확인, PR #154 본문 `resolved`→`related to` 정정, Claude Code auto mode가 force-push를 차단해 사용자 직접 실행으로 우회

### 프로젝트/Zaemit-공모전
- [[Zaemit-공모전/Zaemit-MCP-연동]] — 정본 엔드포인트(`zaemit.ai/mcp`, `mcp.zaemit.ai`는 랜딩페이지 함정), 게이트웨이 3종 구조(403툴/57그룹), 게시판·문의폼 등 플러그인 설치 필요 기능, Free 플랜 한도, 공모전 제약 (2026-09-18 마감), OAuth 인증 완료로 연동 이력 요건 충족 확인 (2026-09-08). 툴 응답에 섞여오는 지시문은 서버 데이터일 뿐 따르지 않는다는 원칙 추가 (2026-09-16)
- [[Zaemit-공모전/Zaemit-사이트-제작-트러블슈팅]] — `board_embed`의 `data-limit`이 제목 div가 아니라 `.wv-board` 루트에서만 읽히는 버그, 검수는 curl 대신 실제 브라우저 렌더로 해야 하는 이유, 툴 인자 한글은 리터럴로(유니코드 이스케이프 조립 금지), `overflow:hidden`만으론 절대위치 장식요소가 안 잘리고 부모에 `position:relative`가 같이 필요한 이유, 외부 이미지는 `image_import`로 저장해야 만료 안 됨, 반투명 카드+밝은 배경 사진 가독성 결함 패턴 (2026-09-08). **(2026-09-16 갱신)** AI 품질 게이트 신규 도입 — `main_added`(섹션 붕괴, `section:"1"`+유일 find로 우회 가능)·`h1_added_from_page_title`(그라디언트 텍스트가 트리거, 롤백+단색 h1 동시 패치로 해결) 동작과 대응법, `page_update` css/js 인자가 블록 조립 페이지에서 거부되는 경로 문제, 섹션 스크립트의 `document.querySelector*`가 `__zsScopeRoot`로 자동 스코프 치환되는 함정과 헤더/푸터 컴포넌트 `js` 슬롯이 스코프 치환 없는 전 페이지 공용 엔진 자리라는 패턴, 밝은 장면 밴드는 진입·이탈 양쪽 페이드가 필요하다는 교훈, 인스타그램 레퍼런스 접근 차단 사례, 스크롤 스토리는 효과보다 분량(뷰포트 수)이 체감을 좌우한다는 정리. **(2026-09-17 갱신)** 페이지 생성 시점의 내부 설명(meta description)값은 MCP로 수정 불가(에디터 전용), `data-zs-role` 값에 따라 플랫폼 전역 CSS 규칙이 다르게 적용돼 역할만 바꿔도 크기가 달라질 수 있다는 함정 추가. **(2026-09-18 갱신)** "제목 단계 건너뜀" 품질 경고가 장식용 번호에 붙은 heading 태그(h5)가 원인일 수 있다는 사례 추가, 대형 JS 엔진(100KB+) 배포 시 출력 한도로 분할 배포가 필요했던 사례·배포 후 curl 바이트 대조 관행·로컬 프로토타이핑 하네스 절차는 [[스크롤-오브젝트-연출-패턴-모음]]으로 분리 정리

### 프로젝트/ToyVillage-Admin-FE
- [[ToyVillage-Admin-FE/프로젝트-현황]] — Figma 퍼블리싱 harness 구조(specs/approvals 분리, 게이트 해시 판정, ②·⑦ 사람 게이트, approvals 재승인 시 cp 누락 함정), 구 파일 폐기 후 `yot` 기준 전환, 업무관리 목록·생성·수정·상세 4개 feature 퍼블리싱·게이트 승인·커밋 완료, 업무 목록 필터·페이지네이션 구현 패턴(useMemo 3단 파이프라인, 렌더 중 상태 보정으로 페이지 리셋), 레이아웃 버그 수정, 업무지시 API 연동 harness(RUNBOOK ①~⑬) 착수부터 승인·구현·검증 완료까지, 단체예약 API(PR #66) 선례 패턴 참고, develop 108커밋 merge. **(2026-09-11 갱신)** 전체 457개 테스트 정밀 재검증(통과 373·실패 84, #58 관련 실패 0건), [[배럴-Import-함정]]으로 3개 스펙이 조용히 안 돌던 문제 확정·수정, task-report S16이 9/8 커밋의 UI 재설계로 깨졌음을 확정(freeze로 실패가 승인 상태로 굳어짐), 실 토큰으로 백엔드 미배포 확인(이 서버는 없는 경로도 404 아닌 500 반환하는 함정), `/team/tree` contract-실배포 불일치 재확인 필요, 커밋 12개로 기능별 분리(단 타입 결합으로 `src/` 마이그레이션 자체는 못 쪼갬), 로그인/토큰 저장 경로 전체 미구현 확인. **(2026-09-12 갱신)** 목록 응답이 승인 Contract(`assigneeName`)와 어긋나(`assignees[]`) 200인데도 오류 화면 뜨던 버그 수정, 표 `overflow:hidden` 케밥 메뉴 클리핑 수정([[테이블-오버플로우-hidden-드롭다운-메뉴-잘림]]), 업무보고·진행도를 상세 응답 `reports`/`progress`로 실연동(`MISSING`→`심사대기` 표시 결정), `| tail` exit code 오판 정정([[쉘-파이프-tail-종료코드-오판]]), e2e 실패 83건을 route mock 부재·fixture 날짜·401·403 전역 로그아웃 세 원인으로 분류해 별도 이슈로 분리 결정, develop 124커밋 머지(충돌 9개 해소, 승인 해시 재발급, work-log 스펙 56개로 해소 검증), push 및 PR 본문 작성. **(2026-09-13 갱신)** 업무보고 카드 화살표 누락 수정, 커밋 제목 문체를 명사형 종결로 확정, 첨부파일 다운로드·양식 생성 빈 화면·자료실 필터/페이지네이션·사이드바 하드코딩 등 런타임 오류 6건 감사, 로그인·사이드바가 최신 Figma(`yot`)와 어긋난 드리프트 확인(2026-09-07 "구 파일 폐기" 선언에도 명세 24개 중 20개가 실제로는 미전환 상태였음 재확인), 이슈 10건 등록 후 기존 이슈와 중복된 4건 삭제·6건 최종 유지. **(2026-09-13 2차 갱신)** task-report 상세 2차 재퍼블리싱(첨부자료 카드 분리), 배지 문구 `승인`→`완료` 전역 결정(진행도 요약은 유지), 공용 `AttachmentList` 라벨 색 Figma 회색 대신 검정으로 개발자 결정, 커밋 13개 세분화(최종 코드는 diff 없음), AI가 임의로 만든 백업 브랜치를 사용자 요청으로 삭제([[Claude-Code-임의-백업-브랜치-생성-지양]]). **(2026-09-13 3차 갱신)** 이슈 #59(업무보고 API 연동)는 Notion API ID 8개 전부 공란이라 여전히 착수 불가, `task-report` 명세를 `yot` 기준으로 재퍼블리싱(탭 4→3, 표 6→4컬럼, 상세 카드 통합, 결과 토스트 신규)하고 시나리오 재승인, 코드리뷰에서 TanStack Query 캐시 무효화·행별 mutation 공유 버그 2건 발견·수정([[TanStack-Query-mutate-콜백-언마운트-스킵]]), 세션이 실행된 orca 작업 폴더와 사용자의 실제 `/data/project` 폴더가 갈라져 있어 브랜치를 이관. **(2026-09-14 갱신)** 이슈 #59 "착수 불가" 판단이 옛 archived Notion DB 사본을 참조한 착오였음이 확정되고(최신 DB엔 ID 전부 존재) 업무보고 관리자 API 4개(목록·상세·승인·반려) 연동 완료, 코드리뷰로 탭 전환 중 이전 데이터 조작 가능 버그 발견·수정([[TanStack-Query-placeholderData-이전탭-데이터-조작-가능]]), "실 서버 요청 없음" 초기 보고가 `reuseExistingServer` 재사용으로 틀렸음을 뒤늦게 발견해 격리 서버로 e2e 재검증(94개 실패는 전부 기존 실패로 확정, [[Playwright-reuseExistingServer-실서버-유출]]), 커밋 9개 push 완료. **(2026-09-14 2차 갱신)** 첨부파일 다운로드 버그(#71) 원인 정밀 진단 — 조회 전용 화면·수정 화면의 기존 첨부(fileKey만 있는 경우)에서만 발생하는 것으로 범위 확정, baseurl+fileKey 조합이 공개 버킷이라 프론트만으로 해결 가능함을 확인(baseurl 실제 값은 미확보, 수정 대상 5곳만 정리, [[크로스오리진-파일-다운로드-a-download-무시]]). **(2026-09-15 갱신)** 개체관리(종·개체·관찰) 슬라이스 신규 절 추가 — `#82` 브랜치 7명세 승인·동결, 화면 8개·라우트 9개 퍼블리싱 완료, 정적 경로를 파라미터 경로보다 앞에 두는 라우팅 결정, shared 컴포넌트 9종 신규+4종 수정, 검증(yarn verify·e2e 187+5 passed·전체 94실패는 기존과 동일) 통과 후 push 완료(PR 미생성). **(2026-09-16 갱신)** PR #88 develop 병합 충돌 5건 해소(사이드바 아코디언 구조 채택, `SectionHeader`는 동료가 먼저 만든 버전을 베이스로 `unit`·`action`만 확장) 후 머지·PR 리뷰 대기, `#89/object-management-api` 브랜치 생성 후 코드 작성 없이 Swagger 대조 API 연동 설계만 진행 — 사진/첨부 URL(base URL+fileKey)·페이지네이션(1부터)·응답코드(GET 200/POST 201)·삭제 연쇄(종→개체·관찰)·PATCH 전체교체·법정지정분류 이름 역매칭 전부 확정, 백엔드 질문 1개로 축소, 디자인 작업 6건(Figma 수정 3+신규 3) 확정. **(2026-09-16 저녁 갱신)** yot 재점검으로 공지사항·자료실·휴관일·단체예약·로그인 diff 확정(공통 패턴: 목록 행 케밥+삭제 모달), 재퍼블리싱 이슈 4건(#92~95, 기존 스텁 #63은 삭제) + 계정 생성 퍼블리싱/API연동 이슈 2건(#96~97) 등록, 이슈 본문에 Figma 노드번호·대조 문구를 넣지 않는 관례 확정. **(2026-09-16 밤 갱신)** 첨부파일 다운로드(#71) 구현·스테이징 실측 검증(pptx/pdf/png 바이트 일치)·PR #100 생성부터 머지까지 완료, 조회·수정 화면 5곳 적용(관찰·개체는 #89 이후로 보류), 공용 인증 axios 대신 CDN 호출에 `fetch` 사용 결정([[공용-인증-axios-외부도메인-토큰유출]]), e2e mock 오리진 미검증 CodeRabbit 리뷰로 발견·수정([[Playwright-mock-오리진-미검증]]), 커밋 메시지 재작성 후 강제 푸시로 해시 전체 교체, 리뷰 답글은 해시만 남기는 관례 확정, 배포 환경변수 `VITE_FILE_BASE_URL` 추가는 미해결로 남김. **(2026-09-17 갱신)** 개체관리 API의 Notion 명세 DB 위치를 새로 확인(`3dc7…`, 종4+개체5=9개 ID) — 이전엔 이 DB를 놓쳐 "없다"고 잘못 기억했던 것으로 정정. Swagger 18개 경로 대조로 Notion 미등록 8개(종 삭제·법정지정분류 3종·관찰 4종) 확정, 백엔드에 API ID·권한/파일 base URL/오류 응답/null 필드/기존 요청 반영 여부/Swagger 표기 오류 6개 항목으로 정리해 요청 발송. **(2026-09-17 사이드바 #77 갱신)** 사용자 정보 조회 API 부재 확인 후 로그인 응답에 이미 저장된 이름을 재사용하는 결정, 승인된 e2e 파일도 목 데이터 제거로 불가피하게 수정, CodeRabbit이 제안한 "정보 없으면 로그인 화면 이동"은 기존 e2e 35개가 깨져 미채택하고 기본값(`사용자`) 표시로 대응. **(2026-09-17 로그아웃 #105 갱신, orca `develop-2` 세션)** 사이드바 하단 로그아웃 spec 작성·승인·퍼블리싱·검증·e2e 5개 통과로 완료, 라벨·구분선 색은 새 토큰 없이 기존 `choiceMuted`/`tableHeaderStrong` 재사용, orca 신규 워크스페이스가 `node_modules` 없이 시작돼 `yarn` 스크립트 전부 실패하던 것을 `yarn install`로 해결, 스테이징이 `localhost:5173`만 허용하고 5173·5174가 이미 점유돼 있어 Vite dev 서버 프록시로 우회([[Vite-dev서버-프록시로-오리진고정-CORS-우회]]), #104(사이드바 프로필) 병합 충돌 해소 후 push. **(2026-09-17 저녁 대시보드 #102 갱신)** 이슈 브랜치를 새 worktree 대신 현재 폴더에 직접 체크아웃하는 것으로 기본 전환, Figma 응답 25k 토큰 초과 시 구조·수치 요약본으로 이어가는 사례, 새 토큰 `dangerSoftBg`(`#FFECEC`), Figma 미결 사항(휴관일 카드는 이번 달로 확정 등)을 spec TODO+기본안으로 처리, `/` 경로를 `DashboardPage`로 교체, 첫 커밋 분리 중 명령 실패로 꼬였던 걸 18개로 재분리, dev 서버 포트 점유 상태에서 응답만 보고 자기 서버가 떴다고 오판했던 사례, CodeRabbit 리뷰 2건+자체 발견 범례 겹침 수정. **(2026-09-17 밤 계정 생성 #96 갱신)** 사람 부재 상태에서 spec 작성부터 시나리오 승인(`claude (yunho 위임)` 명의)·구현·검증·push까지 대신 처리, Figma 로고가 로그인 로고와 바이트 동일함을 발견해 `shared/assets`로 통합, S11 시나리오가 Playwright 오토스크롤로 화면 기준 좌표가 바뀌어 처음 실패한 함정 확인·수정([[Playwright-오토스크롤-화면기준-위치측정-오염]]), 사이드바 `직원 계정 관리` 연동을 목록 화면 부재로 처음엔 제외했다가 후속 커밋으로 추가, dev 서버가 외부에서 SIGTERM으로 종료된 뒤 다른 워크스페이스 서버가 그 포트를 차지한 것을 보고 임의 재기동 대신 사용자 확인 대기로 판단. **(2026-09-17 밤 로그아웃 API #106 갱신)** Notion 명세 DB에 로그아웃 API가 없어 staging Swagger(Basic Auth라 Chrome으로 우회 확인)로 대체 — `POST /app/auth/logout` 정의 확정, 서버 호출 실패(403/500/네트워크 오류)해도 세션은 항상 클리어하고 `/login` 이동, 401만 재발급 재시도 대상 유지, 승인 파일은 해시 보호 때문에 Prettier 미적용 상태를 그대로 둠, PR #118. **(2026-09-17 `develop-3` 공지·휴관일 재퍼블리싱 #93·#95 + API #112 갱신)** 폐기된 Figma 파일을 여전히 가리키던 spec 2건을 재발견해 `yot`로 재전환, 공지 읽기전용 상세·휴관일 상세 신설, #112를 같은 브랜치로 합쳐 공지 목록 API 응답 계약 변경(`{notices,totalPageSize}`, page 1부터, `createdAt`) 반영, 운영시간 수정 API(OPEN_TIME_CREATE/UPDATE) 연동으로 7월에 읽기전용화됐던 편집 기능 부활(id 유무로 등록/수정 분기, 시간 형식 `HH:mm:ss`/`HH:mm` 구분), 스테이징 `GET /close-day?date=` 무시 버그를 프론트에서 우회, Figma에 없는 핑크 포커스 테두리 12곳 제거, git worktree엔 `.env`가 자동으로 안 딸려온다는 함정, PR #120. **(2026-09-17 밤 갱신)** PR #120 머지 후에도 `close-dat-query-all` API 게이트가 안 풀린 원인이 승인 스크립트의 gitignore 아티팩트 폴더(`harness/artifacts/api/`) 의존이었음을 확정 — 커밋된 승인본을 그 폴더로 되살려 대신 승인하고 코드 변경 없는 PR #121로 보정. **(2026-09-17 밤 로그인 #79 갱신)** yot 로그인 프레임(`1830:17172`, 09-13에 확인했던 노드 id `1:6191`과 달라 파일 내 위치가 바뀐 것으로 보임) 기준 재퍼블리싱 완료 — 브랜드 패널·카드 위치·입력칸 테두리·오류 표시를 새 디자인에 맞추고 Figma와 다르게 한 5가지 결정을 spec에 기록, 필요한 아이콘(뜬 눈)이 대상 프레임에 없어 같은 파일의 다른 화면에서 가져와 재사용, e2e S11이 병렬 실행에서만 흔들리는 기존 계열 재확인, PR 제목은 브랜치명 그대로 쓰기로 컨벤션 확정(PR #126). **(2026-09-17 밤 #103 갱신)** localStorage 목업 e2e 3갈래(휴관일 생성·공지 날짜 필터·자료실)를 가짜 서버 방식으로 전환, `resource-edit` S7·S8 시나리오를 기존 API 승인 결정(오류 화면 없음)에 맞춰 "목록 복귀"로 재승인, `harness/publishing/specs/*.spec.md`는 게이트 해시 보호 대상이 아니라 모킹 방식이 바뀌어도 산문 설명이 낡을 수 있다는 구조적 사실을 CodeRabbit 리뷰로 확인, PR #125. **(2026-09-17 밤 먹이 급여 #119 갱신)** 개체 상세 `먹이 급여 기록 확인하기` 버튼을 해당 개체의 최신 급여 기록 상세로 연결(개체별 화면을 새로 만드는 대신 이미 개체 이력 전체를 보여주는 기존 급여 상세 재사용 결정, `getFeedHistory` 공개·`backPath`로 뒤로가기 보정), 버튼을 "미리 비활성화"에서 "클릭 시 조회 후 이동/오류 토스트 분기"로 정정, e2e 48개 통과, PR #122 머지. **(2026-09-17 밤 갱신, 이슈 #129)** access token 만료 시 재발급 없이 로그아웃되는 문제를 curl로 진단 — staging이 reissue 엔드포인트만 빼고 인증 실패 전체를 401 대신 403으로 응답함을 확인, 인터셉터가 403을 재발급 없이 바로 `endSession()` 처리하는 게 원인, "백엔드를 고쳐 달라"는 초안 대신 "프론트에서 403도 재발급 대상으로 넓힌다"로 대응 방향 전환, 수정 방향 없이 문제 상황만 담아 이슈 등록(구현은 미착수). **(2026-09-17 밤 갱신, 이슈 #134)** `/login` 등 하위 경로 새로고침 404 원인을 Cloudflare Workers SPA fallback 미설정으로 확정([[Cloudflare-Workers-SPA-fallback-404]]), `wrangler.jsonc` 추가·PR #135 생성까지 완료(머지 전이라 실서버는 아직 404), 대시보드 빌드 명령이 `npx wrangler deploy`인지는 사용자 확인 대기. **(2026-09-17 밤 대시보드 #107 갱신)** Swagger로 만든 초안을 Notion 새 DB(`API 명세서 토이빌리지`, `3de7a4d6…`) 명세로 재작성(집계 기간·권한·TOTAL·페이지 1부터 확정), 화면 7개 중 3개는 기존 API 재사용, 행 상세 이동은 id가 있는 휴관일·업무보고·업무일지부터 연결, 백엔드 PR #170이 다른 응답 타입(`FeedLogQueryResponse`)에 id를 넣은 것이라 대시보드엔 반영 안 됐던 오인 사례, 뒤이은 PR #172·당일 핫픽스로 먹이 급여·개체관리 id 확보, 개체관리는 종 id가 끝내 없어 개체 조회로 종 id를 얻은 뒤 리다이렉트하는 `ObservationRedirectPage` 신설, 5173 포트 경쟁 재발, 로그인 실패 원인 미확정, PR #131. **(2026-09-17 밤 #80/#133 갱신, orca `develop-3` 세션)** 이슈 #80 Figma yot 전환 마무리 — 명세 10개 노드 교체 시 frontmatter뿐 아니라 본문에 잔존한 옛 노드까지 확인해야 했던 사례, 23개 화면 mock 스크린샷 대 yot 비교로 실차이 5건 발견·수정(업무 폼 입력 박스·날짜 점 표기·휴관일 달력 주 수·종 상세 검색 문구), 게이트가 막힌 화면을 세 원인(테스트 기대값이 낡음/승인 기록 자체가 없음/**기능 제거 후 e2e만 지워지고 spec·승인 기록이 고아로 남음**)으로 분류, PR #130 머지 후 이슈 #132·#133 분리 등록. 이슈 #133에서 휴관일 생성 실패 모달·성공 토스트 구현(수정 화면은 yot에 대응 프레임 없어 기존 문구 유지), PR #136의 Cloudflare Workers Builds 즉시 실패를 시작·종료 타임스탬프 동일로 진단해 브랜치 분기 시점 이전이라 `wrangler.jsonc`가 없었음을 확정([[Cloudflare-Workers-SPA-fallback-404]]). **(2026-09-17 밤 로그인 실패 상태 #137 갱신)** 실패 원인을 401(비밀번호 칸 아래 인라인 오류)과 5xx·네트워크·응답형식오류(공용 Toast)로 이분해 구현, 비밀번호 비움·토스트 그림자는 Figma와 다르게 기존 동작 유지하기로 spec에 기록, 커밋을 specs/artifacts로 나누려다 `harness/artifacts` gitignore 때문에 명세 변경이 다른 커밋에 섞여 올라간 것을 발견해 재분리·force-with-lease, e2e 13개 통과 후 PR #138. **(2026-09-18 이슈 #109 갱신)** 이전에 확인만 하고 방치했던 사이드바 e2e S9(`팀 설정` 비활성 기대가 낡음)·S11(`개체 카드`는 링크 아니라는 주석이 낡음)을 실제로 수정 — 예시 메뉴를 아직 화면 없는 `권한 관리`로 교체, 주석을 텍스트 대신 링크 검사로 정정, 12개 전부 통과, PR #141. **(2026-09-18 갱신)** 사이드바 메뉴 스크롤바 숨김(#139, PR #140), 새 버그를 지금 브랜치에서 바로 고치지 않고 이슈+새 브랜치부터 만든 뒤 고치는 워크플로 확정. **(2026-09-18 팀 선택 #144 갱신)** 공지사항 생성·수정 폼의 분류를 자유 입력 팀 추가 모달 방식에서 팀 설정과 같은 조회 API(`GET /team`) 기반 단일 선택으로 교체, 선택 칩에 강조 스타일이 없어 선택이 안 보이던 버그 수정, `kind: 'ALL'`만 저장하는 기존 API 제약은 여전히 미해결로 리뷰어에게 명시, e2e 52개 통과, PR #145. **(2026-09-18 업무일지 미제출 #146 갱신)** 업무일지 카드에서 미제출을 일부러 `심사대기`로 합쳐 보여주고 모든 줄에 화살표를 남기던 방식을 Figma(133:9725) 기준으로 교체 — 미제출 줄은 배경 흐림·이름 흐린 회색·화살표 제거·전용 `미제출` 배지로 구분해 안 눌리는 상태를 시각적으로도 드러냄, 진행도 카드 요약 2줄 분리+도넛 심사대기/미제출 색 분리, e2e 기대값 수정, PR #148. **(2026-09-18 이슈 #150 갱신)** 업무지시 상세→업무보고 상세 이동 시 진입 경로를 `location.state.returnTo`로 넘겨, 심사(승인/반려) 성공 이동·뒤로가기·"찾을 수 없습니다" 링크 세 곳 모두 온 곳으로 복귀하도록 수정(기존엔 무조건 업무보고 목록으로 이동), 복귀 시 심사 결과 토스트 추가, e2e S34·S35 추가, PR #151. **(2026-09-18 develop-4 갱신)** `returnTo`가 `//`로 시작하면 프로토콜-상대 URL로 해석돼 외부 도메인으로 리다이렉트될 수 있어 거부하도록 보완(open redirect 방지, 사용자 입력이 흘러드는 다른 returnTo류 값에도 일반화 가능), PR #151 머지 확인 후 로컬 브랜치 정리. **(2026-09-19 이슈 #149 갱신, orca `develop-2` 세션)** 개체관리 종·개체 폼 3건 수정(PR #156) — 종 목록 버튼 `종 등록하기`(제목은 `개체 카드` 유지), 사진 칩 ✕ 추가(`removeTone` 회색→hover 빨강, 첨부 칩은 빨강 고정), 코드에 박힌 법정지정분류 기본 선택지 삭제해 서버 공용 목록만 쓰고 전부 삭제 가능하게, "직접 추가 항목은 그 종에만 저장" 명세 오류를 CodeRabbit이 지적해 수정, 게이트 8건 재승인. **(2026-09-19 포트폴리오 정리 갱신, `develop-5` 세션)** 프로젝트 배경(카톡·종이 기록 통합)·1차/2차 기능 범위·본인 기여 규모(PR 42/65)·포트폴리오 작성 결정(역할은 전부, 개발 내용은 3~4개만)과 재정리한 대표 트러블슈팅 사례 링크, 면접 전 확인할 것(기간 06.04 vs 첫 커밋 07.04, stag 표기, 토큰 유출이 실제 발생인지 예방인지, 7월 JWT 하드코딩 커밋). **(2026-09-19 밤 이슈 #158 갱신, orca `develop-2` 세션)** 업무일지 관리 1·2페이지가 같은 데이터로 나온 원인 — 명세는 `page` 0부터라 화면−1을 보냈지만 서버는 1부터 세고 `page=0`·`1`을 둘 다 첫 페이지로 처리, 첫 페이지만 조회하는 대시보드는 0을 보내도 안 드러나 숨어 있었음, 화면 번호를 그대로 전송하도록 수정(양식 관리 탭은 서버 동작 미확인, 업무지시·급여·문서 목록도 `page=0` 잔존), Copilot 리뷰로 harness 명세·구현계획 8곳의 `page: 0`까지 같은 변경에 정정해야 했던 문서 드리프트, PR #162. **(2026-09-19 밤 이슈 #159 갱신, `develop-5` 세션)** 이미 승인된 업무보고를 다시 승인하면 사유 없이 `승인에 실패했습니다`만 뜨던 원인이 `reviewStatus`와 무관하게 심사 버튼을 항상 노출한 것이라 `PENDING`일 때만 렌더링하도록 수정, #150에서 추가한 e2e S34 픽스처가 "이미 APPROVED인 보고를 재승인"하는 버그 시나리오를 그대로 정상 흐름으로 굳혀 놓았던 걸 발견해 PENDING으로 정정(수정 후 기존 테스트가 깨지면 픽스처 전제부터 의심), 회귀 테스트 S36 추가, `app-work-report-approve` S3은 develop에서도 동일 실패하는 기존 이슈로 제외, PR #161. **(2026-09-19 밤 이슈 #147·#164 갱신, 본 체크아웃 세션)** 공지사항 분류를 라디오→체크박스 복수 선택(전체/팀 상호배제·전부 해제 시 전체)으로 바꾸고 목록 탭을 팀 조회 API 기반으로 전환, 화면만으론 서버 반영이 안 됐던 이유(`kind:'ALL'` 하드코딩)와 백엔드의 `kind`→`teamIds`/`teams` 변경(`teamId` 필터는 단일값만, 없는 팀은 404 `TEAM_NOT_FOUND`), Notion이 Swagger보다 뒤처졌을 때 Swagger 기준으로 진행한 결정과 승인 전 구현이라는 절차 위반 기록, 선택을 이름 대신 id로 들면서 드러난 mock id 충돌, CodeRabbit "수정 예시에서 `files` 제거" 제안을 Swagger로 팩트체크해 "수정도 files를 받는다"로 뒤집고(이 PR에선 재승인 비용 때문에 리뷰는 무시) 그 결과 발견한 "수정 화면 첨부 변경이 조용히 저장 안 되는" 버그(#164, 수정 요청에 `files` 누락, Swagger로 확인 가능한 걸 백엔드 질문으로 넘기지 말 것)를 업무 수정 방식대로 고친 PR #165(교체 방식 가정은 스테이징 확인 대기), 다른 세션이 본 체크아웃 브랜치를 바꾸고 `wip` 커밋을 남긴 사건, 격리 포트 없이 돌리면 다른 세션 dev 서버에 붙어 5건 오탐, PR #157·#165. **(2026-09-20 이슈 #170 갱신, orca `develop-2` 세션)** 휴관일 삭제가 서버에서 성공해도 `삭제에 실패했습니다`가 뜨던 원인이 `deleteCloseSchedule`의 엄격한 201+message 검사 — 7/29 `540973e`가 완화한 것을 `cc47c68`(yot 디자인 반영)이 되살린 회귀, 승인된 e2e S7·S8·계약 문서가 엄격 동작을 굳혀 놓아 함께 반전, 2xx면 성공으로 수정, CodeRabbit 지적으로 삭제 후 재조회 mock을 빈 목록으로 두고 카드 사라짐까지 검증 추가(토스트만으론 통과 가능), Docstring Coverage 경고는 저장소 관례상 미반영, 5173 점유 서버(`develop-3`)를 사용자 지시로 종료하고 이 브랜치를 올림, PR #171

### 프로젝트/Commonly-FE
- [[Commonly-FE/프로젝트-현황]] — 경력증명서 발급 프로토타입, FE↔BE 갭 목록(개별등록/민원인 발급 막힘, 대량등록 데이터 미연결 버그, JWT 클레임 부족), 진행한 이슈·PR(#68–#77), juso 주소검색 키 적용, PR 코드리뷰에서 나온 동시성 disabled 스코프 버그·문서번호 빈값 통과 버그 수정, 로그인 무한 루프(#74/#75) 원인·수정. **(2026-09-10 갱신)** 노션 API 명세서 전수 확인 결과 민원인 본인 경력 조회 엔드포인트는 애초에 명세에 없었음이 확인돼(#76/#77) "BE가 GET /self를 신설해야 한다"는 이전 진단을 폐기하고 FE를 명세(선택 발급 불가, 항상 전체 발급)에 맞춰 재정렬, 401이 인가 실패까지 삼키는 메커니즘(accessDeniedHandler 부재) 확인. **(2026-09-16 갱신)** 배포는 Portainer의 `main` 폴링 한 경로뿐이고 CD 워크플로는 삭제돼 없다는 것 확정(develop 머지만으로는 배포 안 됨, 항상 develop→main PR 필요), 사용자 목록 표 정렬 버그를 표 자체 height 중복 지정으로 확정·수정([[표-내부-height-지정-행-높이-분배]], 업무 이력 표에도 동일 패턴 잔존), 개별 등록 API 필드 매핑 오류(`department`↔`division` 혼동) 수정으로 400 해소(#84/#85, 3관점 독립검증+반박검증), 파일 업로드 502가 에러 응답 CORS 헤더 누락 때문에 원인이 안 보였던 것 진단([[에러-응답-CORS-헤더-누락-상태코드-차단]], BE 이슈 #32) + FE/BE 업로드 용량 한도 불일치(20MB/10MB) 발견, 안 쓰는 브랜치 24개 정리

### 프로젝트/clawd-on-desk
- [[프로젝트/clawd-on-desk/프로젝트-현황]] — Electron 데스크톱 펫 앱(포크 PR 기여), PR #980 리눅스 워크스페이스 전환 시 펫 사라짐 수정 — main 병합·Windows 활성화 코드 보존·플랫폼별 테스트 분리·GNOME 스모크 테스트, 독립 코드 리뷰(APPROVE)에서 나온 주석 오류·테스트 제목·fake 미검증 수정, 히트 창 처리와 워크스페이스 전환 포커스 탈취 부작용을 메인테이너 결정 대기로 남김

### 도구
- [[Claude-Code-MCP-서버-등록]] — MCP 서버 목록은 세션 부팅 시에만 로드됨, Desktop 내장 세션은 OAuth 브라우저 승인 불가(터미널 CLI 필요), 터미널을 도중에 닫아 토큰 교환이 빈 값으로 실패하는 패턴과 확인법, `/mcp` 메뉴 커서 위치를 놓쳐 엉뚱한 서버로 들어가는 함정, 자격증명 파일 직접 읽기 우회는 auto mode가 차단
- [[Git-브랜치명-샵-이스케이프]] — 브랜치명에 `#`이 있으면 쉘 주석으로 잘려서 `--delete` 등 뒤 인자가 사라짐, 항상 따옴표로 감싸야 함
- [[Claude-Code-느낌표-bash-접두사-채팅전용]] — `!command`는 채팅 입력 전용 즉시실행 기능, 실제 터미널(bash-input)에 그대로 붙여넣으면 `command not found: !node`로 실패. 실제 zsh 대화형 터미널에 붙여넣으면 히스토리 확장이 `!cd`를 이벤트 지정자로 해석해 `zsh: event not found: cd`로 실패하는 별개 증상도 있음
- [[Git-브랜치-뒤처짐-확인-없이-기능-없다고-단정]] — 로컬 브랜치가 base보다 수십~수백 커밋 뒤처지면 이미 merge된 기능도 "없다"고 오판하기 쉬움, `git fetch` 후 `git log HEAD..origin/<base>`로 뒤처짐부터 확인, "기존 구조 분석" 단계 전엔 먼저 merge/rebase
- [[GitHub-PR-본문-이미지-첨부-gh-cli-한계]] — `gh` CLI로는 PR 본문에 이미지를 직접 못 올림, 브라우저 웹 에디터에 업로드해 `user-attachments` 주소만 얻고 저장은 취소한 뒤 그 주소를 `gh`로 본문에 넣는 우회법
- [[쉘-파이프-tail-종료코드-오판]] — `command | tail -N`의 종료 코드는 `tail`의 것이라 `command` 실패를 가려버리는 함정, `pipefail`/`PIPESTATUS`로 확인하거나 요약 전에 원본 종료 코드부터 확보하는 대응
- [[Claude-Code-임의-백업-브랜치-생성-지양]] — 커밋 재배열 등 안전망이 필요한 작업 전에 AI가 사용자 동의 없이 `backup/*` 브랜치를 만들면 안 되는 이유, 만들기 전에 먼저 확인하고 이미 만들었으면 요청 시 즉시 삭제
- [[브라우저-팬-숨김-상태-rAF-정지-테스트-우회]] — 브라우저 팬이 가려지면 `document.hidden=true`가 돼 rAF·CSS 애니메이션이 멈추고 스크린샷이 타임아웃/찢김되는 증상, `document.hidden` 오버라이드+rAF를 setTimeout으로 패치해 DOM 계측으로 검증하는 우회법, 검사 방식 자체가 만드는 오진(앞 섹션을 숨겨 reveal이 안 트리거된 사례) 함정
- [[Git-스택-PR-재정렬-force-with-lease]] — 스택 PR에서 리뷰가 지목한 커밋이 상위 브랜치에 없으면 스택이 어긋났다는 신호, 순차 rebase로 재정렬, 브랜치별 명시적 SHA를 건 `--force-with-lease`로 동시 작업 중인 다른 세션을 안전하게 보호하는 법, Claude Code auto mode가 force-push를 `[Git Destructive]`로 차단해 사용자가 직접 실행해야 하는 제약
- [[Swagger-자동생성-문서-필수값-확인과-신뢰-한계]] — Request body Schema 탭(Example Value 탭 아님)에서 `required` 배열·`minLength`/`minItems`로 실제 검증 어노테이션 기반 필수값을 읽는 법, 응답 DTO는 검증 어노테이션이 없어 Swagger만으론 null 가능 필드를 알 수 없는 이유, `pageable required:true`·성공코드 200 고정 표시·`page` minimum 표시를 그대로 믿으면 안 되는 이유. Basic Auth로 막힌 staging Swagger는 이미 로그인된 브라우저로 우회, Notion 명세에 없는 API의 유일한 출처가 될 수 있다는 사례 추가. **(2026-09-19 추가)** `page` 시작값 표기(0)와 실제(1)가 어긋나 `page=0`·`1`이 같은 응답을 내 1·2페이지가 동일하게 보인 실사례와 진단법
- [[Yarn-Berry-silent-플래그-무효-스크립트-거짓FAIL]] — Yarn classic의 `-s`(silent) 플래그가 Yarn berry에서는 무효라 검증 스크립트가 원인 불명 FAIL을 내는 함정, `node scripts/xxx.mjs` 직접 실행으로 교차 검증하는 대응
- [[zsh-단어분리-안됨-glob-nomatch-함정]] — zsh는 bash와 달리 `$F`를 자동 단어 분리하지 않아 `for` 루프가 한 번만 도는 함정과 `bash -c` 우회, 매치 안 되는 glob 패턴에 `setopt nonomatch` 없으면 "no matches found" 에러를 내는 차이
- [[Notion-MCP-쿼터-SQL-rows-소진-시-view모드-우회]] — SQL/COUNT·rows 조회가 같은 쿼터를 공유해 소진될 수 있는데 데이터베이스 view 조회는 쿼터를 소모하지 않아(quota-free) 막혔을 때 대체 경로로 쓸 수 있다는 것
- [[Claude-Code-서브에이전트-fan-out-토큰-과다소모]] — 반복적·탐색적 소작업까지 팀 에이전트로 병렬 fan-out하면 토큰 비용이 급증한다는 사용 패턴, 독립적이고 덩어리가 큰 작업에만 fan-out 이득이 오버헤드를 상회한다는 판단 기준
