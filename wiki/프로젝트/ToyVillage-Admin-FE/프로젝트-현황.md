---
tags: [project, toyvillage, frontend, figma-publishing]
updated: 2026-09-12
---

# ToyVillage-Admin-FE 프로젝트 현황

관리자 프론트엔드 저장소. Figma 디자인을 코드로 옮기는 작업을 **자체 harness 파이프라인**(`.claude/skills/publishing`)으로 게이트 처리한다. 진실의 원천은 `harness/publishing/RUNBOOK.md` / `design-rules.md`이고, 이 페이지는 그 구조에서 재사용 가치가 있는 부분만 요약한다.

## 퍼블리싱 harness 구조

파이프라인은 ①추출 → ②시나리오 승인(**사람**) → ③퍼블리싱 → ④검증 루프 → ⑤e2e 변환 → ⑥… → ⑦육안 확인(**사람**) 순서다. AI는 ②·⑦ 게이트를 자동 통과시키지 않는다.

- **`harness/publishing/specs/`** — 화면 스펙. AI가 최신 Figma 반영을 위해 갱신 가능.
- **`harness/publishing/approvals/`** — 승인 상태(시나리오 초안 + `*.approved.json`). AI가 **직접 수정 금지**, 공식 스크립트(`node scripts/approve.mjs`)로만 바뀐다.
- **게이트 판정**: `approved.json`의 `scenarioHash`(승인된 시나리오 초안의 해시)와 `e2eHash`(e2e 테스트 파일의 해시)가 각각 현재 파일과 일치해야 `yarn harness:gate <feature>`가 `valid: true`를 낸다. 둘 중 하나라도 어긋나면(초안을 고쳤는데 재승인 안 함, e2e를 고쳤는데 재freeze 안 함) 게이트가 막힌다.
- **시나리오를 다시 승인하면 `e2eHash`가 `null`로 초기화**된다 — 재승인 직후엔 `e2eFrozen: false` 상태이고, 구현 후 e2e를 승인 시나리오 기준으로 다시 변환하고 `--freeze`해야 게이트가 완전히 닫힌다.
- **공유 컴포넌트 변경 시 영향 범위를 먼저 확인**해야 한다 — `DataTable`은 5개 테이블(공지·자료실·예약·업무 등)이 공유하고, `TaskPriorityBadge`/`TaskStatusBadge`는 `task-report` spec과 공유라, 한 spec의 요구로 전역 기본값을 바꾸면 다른 화면이 깨질 수 있다. `design-rules.md §4` 기준 이런 변경은 중간 게이트 승인 범위. (실제로 업무관리 목록의 우선순위 배지를 pill 모양으로 바꾸자 `task-report`도 같이 바뀌어버려, `TaskReportPriorityBadge`를 신설해 분리한 사례가 있다.)
- **`approve.mjs`는 `approvals/`에 이미 있는 파일을 승인할 뿐, 그 내용이 최신 초안인지는 검증하지 않는다.** 새로 쓴 시나리오 초안은 `harness/artifacts/publishing/<feature>.scenario-draft.md`에 있는데, 이걸 `cp`로 `approvals/`에 옮기는 걸 빼먹고 `approve.mjs`만 돌리면 게이트는 `valid: true`로 뜨지만 실제로는 **옛(폐기된 파일 기준) 초안에 새 시나리오 id만 얹혀 승인된, 의미 없는 통과**가 된다. 이 실수가 한 세션에서 세 번 반복됐다 — cp와 approve를 한 블록으로 묶어 안내하면 사람이 cp 부분만 빠뜨리기 쉬우므로, 재승인을 요청할 땐 `cp` 명령과 `approve.mjs` 명령을 분리해서 순서대로 안내하고 실행 여부를 따로 확인하는 게 안전하다.

## Figma 파일 전환 (2026-09-07)

구 파일 `toyvillage-dev`(`fkbMQaiPeIufKzjXXoWAPS`)가 **폐기**되고 `yot`(`P7Jhnu8qV5m9q2QJNzkwAN`)으로 전체 화면이 재정리됐다. `업무관리 · 목록`은 `1:3267`. spec의 Figma 참조가 바뀔 땐 frontmatter의 `fileKey`/`nodeId`를 갱신하고 게이트 ② 재승인이 필요하다 — 표 골격이 같아 보여도 컬럼 구성·배지 색처럼 실제로 바뀐 부분이 있을 수 있으므로 스크린샷만으로 넘겨짚지 말고 실측이 필요하다.

관련: [[Figma-MCP-팀별-호출-한도]] (이 전환 과정에서 파일별 호출 한도에 걸렸던 사례), [[Git-브랜치명-샵-이스케이프]] (이 저장소 브랜치 컨벤션 `#이슈번호/설명`에서 실수하기 쉬운 부분)

## 커밋 컨벤션

`<type>: <한국어 설명>` 형식, 스코프 없음, 본문·트레일러 없이 **제목 한 줄만**(예: `feat: DataTable에 표 외형 옵션(appearance) 추가`). 원자 단위로 나눈다 — 예: DataTable 옵션 API 추가, 표 적용, 탭/버튼 적용, 게이트 해시 재freeze, spec 재작성, 시나리오 재승인을 각각 별도 커밋으로.

이미 원격에 push된 커밋을 `--amend`로 고치면(예: 실수로 본문이 들어간 걸 제목 한 줄로 정리) 로컬과 원격이 같은 내용의 다른 SHA로 갈라진다 — GitHub에는 같은 커밋이 두 개처럼 보이고, 정리하려면 `git push --force`가 필요하다. 같은 브랜치를 이미 pull 받은 사람이 있으면 `git fetch && git reset --hard origin/<브랜치>`로 맞춰야 한다.

## 브랜치 네이밍

`#이슈번호/설명` 형태(예: `#58/task-management-api-publishing`). 원격 브랜치를 새로 만들 때 접두사 없이 올리면 나중에 다시 이름을 맞춰야 하므로 처음부터 컨벤션을 맞추는 게 낫다.

## 알려진 이슈 (2026-09-07 기준)

- `notice-list`·`resources-list`의 e2e 10건이 이미 실패 상태 — 특정 브랜치의 기존 문제이며 새 변경의 회귀가 아님(stash 후 재실행으로 동일 실패 확인됨).
- `tokens.ts`의 `font.body`(`Wanted Sans`)가 저장소에 실제 폰트 파일/`@font-face` 없이 선언만 있어 전 화면이 `system-ui`로 렌더됨 — Figma 대비 글자 폭 미세 오차의 원인.
- `yarn playwright test`를 필터 없이 전체 실행하면 수집 단계에서 `form-data` 링크 에러로 죽는다 — stash한 clean 트리에서도 재현되는 기존 문제라 변경과 무관. 하네스 진입점인 `yarn verify:e2e <feature>`와 파일 지정 실행은 정상 동작한다.

## 업무관리(task) 슬라이스 진행 상태 (2026-09-08 갱신)

- **`task-list`(목록)**: 퍼블리싱·커밋(`160faab`, 21개 파일)·⑦ 육안 확인·push까지 완료. `#58/task-management-api-publishing` 브랜치.
- **`task-create`/`task-edit`/`task-detail`**: 모델 전환(`assigneeIds[]`+팀 트리, `visibility` 제거)부터 라우트 분리(`/tasks/:id` 읽기전용 상세, `/tasks/:id/edit` 수정)까지 퍼블리싱 완료. `yarn verify` 통과, e2e 78/78(create 20·edit 16·detail 19·list 23). `develop` 브랜치에 4개 커밋(`af1dfc6`~`2e46570`)으로 push 전 상태. **담당자 모델 전환이 목록·생성·수정·상세 네 화면을 동시에 건드려서, 중간 커밋 3개는 단독으로는 빌드되지 않는다** — 리뷰하기 좋은 단위로 나눈 것이지 bisect 가능한 형태는 아님(사용자가 이 트레이드오프를 확인하고 유지, squash는 안 함).
- 담당자 다중화로 `팀 트리` mock 데이터(4개 팀 18명)가 필요해졌는데, Figma엔 사육팀 4명만 실명이 보이고 나머지 14명은 인원수만 있어 이름은 AI가 임의로 채움(실측 아님, 합계만 Figma와 일치) — 실제 API 연동 시 교체 대상.

### 퍼블리싱 후 이어진 수정 (같은 슬라이스, 2026-09-08)

- **레이아웃 버그 3건** 발견·수정: `<legend>`가 flex fieldset 최상단에 붙는 문제, 좁은 폭(981~1135px)에서 상세 요약행 고정폭 그리드가 넘치는 문제, 담당자 트리 행 간격이 Figma 규격(72px)보다 좁던 문제. 원인·해결은 [[CSS-fieldset-legend-flex-패딩-무시]] 참고.
- **작성/수정 폼 카드 분리**: Figma가 "내용 카드" 1장(제목+상세+첨부)에서 독립 카드 3장(제목/상세 내용/첨부자료)으로 바뀜 — 상세 화면(`133:9725`)은 변경 없었고, 바뀐 건 폼(`145:12270`)이었다는 걸 노드 재조회·`get_screenshot` byte-diff로 확인 후 반영.
- **UX 결정 (모두 개발자 지시로 Figma 규격에서 의도적으로 벗어남, 각 spec에 결정 사항으로 기록)**:
  - 상/중/하 우선순위 pill 선택색을 Figma 회색(`#DDDDE3`)에서 accent 파랑(`accentBg`/`accent`)으로 변경.
  - 수정 화면 진입 시 담당자 트리를 **모든 팀 접힘**으로 시작(기존엔 선택된 팀만 펼침) — 게이트 ② 재승인(S3) 필요했고 승인받음.
  - 제목·상세 업무 내용 입력 필드의 `:focus-visible` 아웃라인 제거(다른 컨트롤은 유지).
- **업무 상태 모델 변경**: `반려` 상태를 없애고 세 번째 상태를 **지연**(`OVERDUE`)으로 바꿈. 저장값은 `IN_PROGRESS`/`DONE` 두 가지뿐이고, `OVERDUE`는 `resolveTaskStatus(task, today)` 한 곳에서 완료기한 기준으로 **파생**시킨다 — 완료된 업무는 기한을 넘겨도 완료 탭에서 안 사라짐. 목록을 4행→10행/페이지로 확장(mock 14건)하고 `지연` 탭(S24)도 추가. 이후 Figma 탭바도 동일하게 갱신된 걸 확인.
  - **(2026-09-09 갱신) 위 모델은 폐기 대상.** API 명세가 확정되면서 `COMPLETED`/`EXPIRED`는 서버가 계산해 내려주는 값(완료 조건이 "담당자 전원 승인"이라 클라이언트가 재현 불가)으로 바뀌었다 — 아래 "업무지시 API 연동" 절 참고.
- **업무 보고 목록 정렬**: Figma는 승인·승인·반려·심사대기 순으로 보이지만 예시 데이터로 판단, 구현은 mock 제출 순서를 유지하기로 결정 — 실제 API 연동 시 `/api` 스킬에서 재검토 대상으로 남김.
- **남은 미결 — `task-report` spec S16 재승인 필요**: 승인된 시나리오의 진입점(`업무 보고 상세조회` 버튼)이 이번 변경으로 상세 화면의 담당자별 업무보고 목록으로 대체되어 사라짐. 프리즈된 e2e를 임의로 고치지 않고 `specs/task-report.spec.md` 미결 사항에 재승인 대상으로만 기록.

## 업무 목록 필터·페이지네이션 구현 패턴 (`TaskListPage.tsx`)

- **탭 = 문자열, 상태는 룩업 테이블로 매핑**: `tabStatuses: Record<string, TaskStatus | null>`로 탭 이름을 곧바로 상태 키로 쓴다. `null`은 "필터 없음"(전체 업무 탭). 탭 표시 자체는 문자열만 올려주는 `CategoryTabs`(순수 표시용)가 담당.
- **필터링 파이프라인은 `useMemo` 3단 분리**: `allTasks → items(resolveTaskStatus로 표시용 status 부여) → filtered(tabStatuses 일치 여부) → tasks(페이지 slice)`. 각 단계가 별도 `useMemo`라 탭만 바꾸면 `items` 재계산 없이 `filtered`부터 다시 돈다.
- **`today`를 한 번만 `useMemo`로 잡아 필터와 표에 동일하게 전달**: 탭 필터의 기준일과 `TaskTable`의 기한 표시(`today` prop)가 같은 값을 쓰도록 해서, 자정을 넘겨 렌더가 다시 일어나도 필터 기준과 화면 표시가 어긋나지 않게 한다.
- **탭 전환 시 페이지 리셋은 `useEffect`가 아니라 "렌더 중 상태 보정" 패턴**: `const [prevActive, setPrevActive] = useState(active); if (prevActive !== active) { setPrevActive(active); setPage(1) }`. `page` state 자체는 건드리지 않고 `currentPage = Math.min(page, pageCount)`로 clamp하기 때문에, 삭제로 목록이 줄어 페이지 수가 감소해도 빈 화면이 뜨지 않는다.
- 날짜 비교는 `dueDate < today`처럼 `YYYY-MM-DD` 문자열 사전순 비교로 처리 — `Date` 객체 변환 없이 동작한다(`resolveTaskStatus`, `src/entities/task/model/status.ts:16`).

## 업무지시(task) API 연동 착수 (2026-09-09)

퍼블리싱과 별개 harness(`.claude/skills/api`, `harness/api/RUNBOOK.md` ①~⑬ 단계)로 진행. ①spec 작성 → ②③Notion 원문 수집 → ④Contract 생성 → ⑤validate → ⑥기존 구조 분석 → ⑦구현계획·테스트시나리오 → ⑧개발자 승인 → ⑨구현 순서이고, **개발자 승인(⑧) 전에는 API 코드·테스트를 작성하지 않는다.**

- **Notion API 명세서가 세션 도중 DB째로 여러 번 교체됐다** — 처음 조회한 `API 명세서 (1)`은 이미 archived(삭제)된 DB였고, 라이브는 이름이 비슷한 `API 명세서 토이빌리지`였다. 이후로도 라이브 DB 자체가 통째로 새 사본으로 두 차례 더 교체됨(콘텐츠는 유지, 페이지 URL만 갱신). → **spec/Contract 작업 시작 전 매번 "지금 조회한 DB가 살아있는(비-archived) 최신 DB인지"부터 확인해야 한다.** 이름이 같아 보여도 다른 데이터베이스일 수 있다.
- **최종 확정된 API 설계**:
  - `GET /team/tree` (`TEAM_QUERY_TREE`, 신설) — `{ totalMemberCount, teams: [{ id, name, memberCount, members: [{id, name, position}] }], unassigned: {...} }`. 담당자 선택 트리(팀별 그룹 + 3상태 체크박스 + 직급 표시)의 데이터 소스. `position`은 `null` 가능.
  - `GET /tasks/{id}` 응답에 `assignees: [{id, name, position}]` 추가 — 수정 폼이 기존 담당자 선택을 복원하는 데 필요. `POST/PUT /tasks`는 `assigneeIds: number[]`로 저장.
  - 상태 enum이 `IN_PROGRESS`/`COMPLETED`/`EXPIRED`로 확정(서버가 계산해서 내려줌: `COMPLETED`=담당자 전원 APPROVED, `EXPIRED`=미완료+기한초과) — 클라이언트에서 재현 불가능한 조건이라 기존 `resolveTaskStatus`(완료기한 기준 파생)는 **제거 대상**, 코드의 `DONE`→`COMPLETED`, `OVERDUE`→`EXPIRED` 개명 필요.
- **선례로 확인된 패턴 — 단체예약(PR #66, `develop`에 이미 merge)**: `GET /reservation/assigned-employee/{reservationId}`가 `{ assigned: [...], assignable: [...] }`로 후보 전원 + 초기 배정 상태를 한 번에 주고(`reservationId: -1`이면 "저장 전" 신규 생성용 전원-assignable 규약), 배정 추가/취소는 로컬 state로만 관리하다가 저장 시 `appAdminIds` 배열을 생성/수정 요청 바디에 실어 한 번에 보낸다 — 명세에 있는 개별 권한 부여/삭제 API(`RESERVATION_PERMISSION_CREATE`/`DELETE`)는 실제로 안 쓴다. `usePermissionAssignment` 훅과 `src/entities/reservation/api/` 모듈 구조가 업무지시 구현의 참고 대상.
- **develop을 먼저 merge하기로 결정**: 브랜치가 8/28 분기 이후 `develop`보다 108 커밋 뒤처져 있었다(주로 PR #66). ⑥단계가 "기존 API 구조 분석"이라 최신 패턴(위 예약 API 구조)을 봐야 의미가 있으므로, ⑥ 진입 전에 `git merge origin/develop`(충돌 0건, typecheck 통과, 로컬에만 존재·미push)로 반영. 뒤처진 브랜치에서 "이 API/기능이 없다"고 여러 차례 오판했던 경위는 [[Git-브랜치-뒤처짐-확인-없이-기능-없다고-단정]] 참고.
- **PR 전략**: 이슈 #58("업무 관리 api 연동 및 디자인 수정") 하나에 퍼블리싱(17커밋)+API 연동을 한 브랜치·한 PR로 유지하기로 결정 — API 연동이 퍼블리싱한 화면(`TaskForm`의 mock 저장, `taskMembers` mock, 상태값)을 그대로 다시 고치므로 PR을 나눠도 리뷰 대상 코드가 거의 겹친다.
- **진행 상태(폐기, 아래 "승인·구현·검증 완료" 절 참고)**: ~~spec 5개 작성·Contract 생성·validate 전부 통과, 다음은 ⑥ 프로젝트 구조 분석~~ → 이후 세션에서 ⑥~⑪까지 완료됨.
- **남은 확인 항목(구현을 막지 않음, 2026-09-10 갱신)**: ~~`GET /tasks/{id}`의 `assignees`가 담당자 전원인지~~ → **해소**(아래 참고, 전원 반환 백엔드 확인 완료). 남은 건 `totalMemberCount`의 `unassigned` 포함 여부, `TASK_QUERY_ALL`의 403 정의 누락, `members[]` 정렬 기준, `reports[].status` 허용값(업무보고 연동 때 필요).

## 업무지시(task) API 연동 — 승인·구현·검증 완료 (2026-09-10)

⑧ 개발자 승인 → ⑨ 구현 → ⑩ 정적 검증 → ⑪ Mock 테스트까지 5개 feature(`team-query-tree`/`task-query-all`/`task-query`/`task-create`/`task-update`) 전부 완료. 구현 순서는 의존 관계상 `team-query-tree`(담당자 id 유일 출처) → `task-query-all`(상태 모델 확정) → `task-query`(수정 폼 초기값) → `task-create` → `task-update`.

**승인 전 확정한 설계 결정 4가지** (모두 코드에 반영됨):
1. **상태 값 개명**: `DONE→COMPLETED`, `OVERDUE→EXPIRED`. 서버가 세 상태를 모두 계산해 내려주므로 `resolveTaskStatus`·`taskToday`를 삭제했고, `entities/task-report`의 mock `taskStatus` 12곳까지 개명이 파급됨. 목록의 완료기한 위험색 표시는 유지하되 기준일 계산은 `TaskTable` 내부 순수 헬퍼로 옮김.
2. **`src/entities/team` 신설**: entities끼리 import하지 않는 이 저장소 규칙 때문에 팀 트리(`GET /team/tree`)를 task entity에서 분리. 그 여파로 `findTaskMember`가 사라지고 `task-report`의 mock이 `assigneeName`을 직접 가짐(mock 내부에서만 닫히는 변경).
3. **`전체 직원` 체크박스 3상태 판정 분리**: 표기 숫자는 `totalMemberCount`를 쓰되, 체크 판정 분모는 실제로 렌더된 직원 수로 따로 둠. `totalMemberCount`가 미배정 인원을 포함하는지 여부가 백엔드 미확인 상태라, 두 값을 하나로 묶으면 "전체 선택"이 영원히 `mixed`(부분 선택) 상태에 갇히는 걸 막기 위한 안전장치. 두 값이 실제로 같으면 동작 차이 없음.
4. **`AttachmentField` 확장**: 수정 화면이 기존 첨부의 `fileKey`를 재전송해야 하는데 기존 shared 컴포넌트는 파일명만 노출했음. optional prop `initialFiles`/`onFileItemsChange` 2개를 추가하고 실제 업로드 로직은 `TaskForm`(도메인 레이어)이 담당 — shared 컴포넌트는 도메인에 의존하지 않는 원칙 유지, 기존 호출부는 영향 없음.

**백엔드 확인으로 해소된 사항**: `TASK_QUERY.assignees`가 담당자 **전원** 반환임을 개발자가 직접 확인. 일부만 왔다면 수정 화면이 담당자를 복원 못한 채 `PUT` 전체 교체로 담당자가 유실되는 위험이 있었음 — 확정으로 문서(spec/Contract/final-report) 갱신 후 `task-query`/`task-update` 재승인·재동결(`--freeze`)함.

**검증 결과**:
- `harness:api:validate`·`gate`·`policy`(task-delete 포함 6건) 전부 통과, `yarn lint`/`typecheck`/`build` 통과.
- 신규 Mock 시나리오 70건(12·12·14·15·17) 전부 통과.
- 퍼블리싱 회귀: task-list 24·task-detail 19·task-create 20·task-edit 16 통과. 공용 route mock(`tests/e2e/support/task-api.ts`)으로 교체.
- `tests/e2e/api/task-delete.spec.ts`도 함께 손봄 — 상세 조회가 mock에서 API로 바뀌고 삭제 UI가 케밥 메뉴로 옮겨져 진입 경로가 깨져 있었음(이전 퍼블리싱 변경 때부터의 기존 실패). 경로만 고치고 `--freeze` 재동결, 9건 통과.
- 실제 백엔드/staging 서버 호출은 5건 모두 `real_server.enabled: false`로 **미실행**.

## 알려진 이슈 (2026-09-10 추가)

- **이 개발 환경에서 5173 포트를 다른 프로젝트(JOBIS-FE-V2)가 점유**하고 있으면 Playwright가 그 서버를 재사용해 이 저장소의 전체 e2e가 404로 실패한다 — `PLAYWRIGHT_BASE_URL=http://127.0.0.1:5199` 처럼 다른 포트를 명시해 우회. 여러 프론트엔드 프로젝트를 병행할 때 재발 가능.
- `yarn playwright test`로 필터 없이 전체 실행 시 `close-schedule-edit`·`operating-hours-edit`가 `@/entities/...` import로 모듈 링크 오류를 내며 수집 자체가 실패한다 — 기존에 알려진 "전체 실행 시 수집 단계에서 죽는다" 문제(위 항목)와 같은 계열의 사전 존재 이슈. **(2026-09-11 해소)** 근본 원인 확정 후 3개 파일 수정·커밋 완료 — 자세한 원인·해결은 [[배럴-Import-함정]] 참고.
- **휴관일(close-dat) 화면 e2e 20건 + Authorization 헤더 미설정 3건**이 이번 작업과 무관하게 기존 실패 상태(변경분 stash 후 재실행해 동일 실패 확인). close-dat 쪽 근본 원인은 [[테스트-픽스처-고정날짜-현재월-필터-노후화]] 참고 — 화면이 "이번 달"만 렌더링하는데 테스트 픽스처가 2026-07/08로 고정돼 있어 달이 바뀌며 자동으로 깨지기 시작한 시간 의존 버그(코드 변경과 무관, 마지막 관련 커밋은 `5539d4f`, 2026-07-31). Authorization 3건(`notice-delete`/`notice-update`/`file-create` 각 S1)은 테스트가 `beforeEach`에서 `accessToken`을 localStorage에 넣지 않은 채 `Bearer` 헤더를 단언하는 테스트 쪽 결함으로 추정.

## 업무지시 API 연동 — 백엔드 머지 대기 확인 및 커밋 분리 (2026-09-11)

- **전체 스펙 정밀 재검증**: 57개 파일/457개 테스트를 개별로 실행해 통과 373·실패 84로 확정. 실패 내역은 세 갈래다 — ① 낡은 목-시대 스펙(76건, notice/resource/close-schedule/operating-hours 계열, 실 API 전환 이후의 기존 문제로 이번 파트와 무관), ② 그중 [[배럴-Import-함정]]에 가려 있다가 드러난 21건(①과 원인 중복), ③ `task-report` S16 1건. **이번 파트(#58)와 직접 관련된 실패는 0건**이다.
- **배럴 import 크래시 3개 파일 수정·커밋 완료**: `close-schedule-create`/`close-schedule-edit`/`operating-hours-edit`가 엔티티 배럴을 거치며 axios를 끌어와 수집조차 안 되던 문제. 원인·해결은 [[배럴-Import-함정]] 참고. 이번 파트 범위 밖이지만 "이미 했으니 유지"로 결정, 해당 3개 spec도 함께 재freeze.
- **`task-report` S16 근본 원인 확정**: 이번 파트가 만든 문제가 아니라 `abfd491`(2026-09-08, "업무 상세를 읽기 전용 화면으로 신설")에서 `TaskReportSummaryCard`로 재설계하며 `업무 보고 상세조회` 버튼이 사라진 것이 원인. 동결 스펙은 여전히 그 문구를 찾아 타임아웃한다. 스펙 헤더 규칙(`실패 시 코드를 수정한다`)상 코드 쪽이 접근성 이름을 복구해야 한다(예: 버튼에 `aria-label` 추가) — 아직 미조치. `mockTaskApi(page)` 추가는 #58이 한 게 맞지만, 이는 상세 화면이 API 연동으로 바뀌며 필요해진 별개 조치이고 넣어도 낡은 locator 자체는 여전히 실패한다.
- **⚠️ freeze로 실패가 "승인된 상태"로 굳어짐**: 개발자 지시로 8개 feature(`task-list`/`task-create`/`task-detail`/`task-edit`/`task-report`/`close-schedule-create`/`close-schedule-edit`/`operating-hours-edit`)를 일괄 재freeze했다. 재freeze 전에는 e2eHash 불일치로 게이트가 아예 막아 실패를 가려줬지만, freeze 후에는 게이트는 통과하고 테스트만 실패로 뜨는 상태로 바뀐다 — `task-report`는 게이트 통과·테스트 1건 실패(S16)로 남아 있다.
- **백엔드 미배포를 실제 토큰으로 최종 확인**: 발급받은 액세스 토큰(만료 1시간)으로 스테이징을 직접 호출. `GET /tasks`가 토큰 없이 403(인증 실패), 토큰 있으면 500으로 바뀜(인증은 통과, 경로가 없음). **이 서버는 존재하지 않는 경로도 404가 아니라 500을 반환**한다(`GET /definitely-not-real`도 동일 500) — "미배포"와 "서버 오류"가 응답만으로 구분되지 않는 함정이 있어, 배포 여부 확인은 항상 살아있는 다른 엔드포인트(`/documents`·`/notice`·`/close-day`·`/reservation`·`/team` = 200)와 대조해서 판단해야 한다. `/team/tree`는 400인데, 이는 `/team/{id}`가 `tree`를 정수 id로 파싱 시도해 나는 것(`/team/abc`와 동일 응답)이지 별도 라우트가 존재하는 게 아니다 — 승인된 contract의 `/team/tree` 경로가 실제 배포와 맞는지 **머지 후 백엔드에 재확인 필요**.
- **커밋을 기능별 12개로 분리**: API 클라이언트·mock 헬퍼·스펙 5개·배럴 수정을 각각 별도 커밋으로 나눴다. 단, **`model/types.ts`의 상태 enum 변경이 `mock.ts`를 거쳐 5개 화면에 동시에 물려 있어 `src/` 마이그레이션 자체(`feat: 업무지시 목록·상세·생성·수정·삭제를 API 연동으로 교체하고 목 제거`)는 더 쪼갤 수 없었다** — 구/신 타입을 공존시키는 과도기 코드를 넣는 게 오히려 더 지저분해져 하나의 커밋으로 유지하기로 결정. `entities/team`, `shared/ui/AttachmentField`처럼 단독으로 typecheck되는 조각만 별도 커밋으로 먼저 뗄 수 있었다.
- **로그인·토큰은 여전히 미구현**: `accessToken`을 저장하는 코드가 앱 전체에 없다(`src/shared/api/auth.ts`는 읽기만 함). `submitMockLogin`은 300ms 대기 후 그냥 성공 처리할 뿐 서버 호출도 토큰 저장도 안 한다. 로그인 API 명세 자체가 `harness/api/specs/`에 없어 작업 대상으로 잡혀 있지 않다. 지금은 브라우저 콘솔에서 `localStorage.setItem('accessToken', <토큰>)`으로 수동 주입해야 화면을 확인할 수 있다(반드시 앱이 뜬 origin 탭의 콘솔에서 — localStorage는 origin별로 분리됨, 5173을 다른 프로젝트가 점유 중이면 다른 포트로 뜬 탭을 써야 함). 실 로그인 구현엔 ① 로그인 API 명세 추가 ② `submitMockLogin`을 실제 구현으로 교체 ③ 만료 처리(`axios.ts`의 빈 응답 인터셉터가 그 자리로 예비돼 있음) 세 가지가 필요 — 별도 이슈 대상.

## 목록 스키마 불일치·케밥 메뉴 클리핑·업무보고 연동·develop 대형 머지 (2026-09-12)

- **목록 응답이 승인된 Contract와 어긋나 "불러오지 못했습니다" 오류**: `TASK_QUERY_ALL`은 아직 노션 명세가 `assigneeName`(문자열)인데 서버가 상세 조회와 같은 `assignees: [{id, name, position}]` 배열을 내려주기 시작함. `taskApi.ts`의 런타임 가드가 `typeof assigneeName === 'string'`을 요구해 200 응답을 오류로 처리 — HTTP 상태만 보면 정상이라 네트워크 탭으로는 원인이 안 보이고, 응답 검증 가드 코드를 봐야 잡히는 유형. 서버 기준으로 Contract를 갱신(`assigneeName`→`assignees[]`)하고 재승인, 대표 이름은 `assignees[0]?.name` + `외 N명`은 `assigneeCount - 1` 유지, 옛 스키마 응답을 오류로 처리하는 회귀 시나리오(S13)도 추가. 노션 명세 갱신 요청은 `task.backend-questions.md` 4번에 기록(서버가 명세보다 앞서 나간 상태).
- **표 `overflow: hidden`이 케밥(⋮) 메뉴를 잘라 `수정`/`삭제`가 안 보이던 버그 수정**: 원인·해결 패턴은 [[테이블-오버플로우-hidden-드롭다운-메뉴-잘림]] 참고. 회귀 테스트(`row-action-menu-clipping.spec.ts`)는 `toBeVisible()`이 조상 클리핑을 못 잡는 함정 때문에 hit-test 방식으로 작성 — 자세한 이유는 [[Playwright-toBeVisible-조상-클리핑-미탐지]] 참고.
- **업무 상세의 업무보고·진행도를 실제 데이터로 연동**: 추가 API 호출 없이 `GET /tasks/{id}` 응답의 `reports[]`(담당자별 보고 현황, 미제출은 `workReportId: null`)·`progress`를 그대로 사용. 화면은 승인된 배지 4종(`승인`/`반려`/`심사대기`/`재제출`)·도넛 3조각 규격을 유지하기 위해 서버의 `MISSING`(미제출)을 `심사대기`로 표시하고 진행도도 `심사대기 = progress.pending + progress.missing`으로 합산하기로 결정(개발자 지시, 기존 `재제출` 합산 규칙과 동일 방식) — 서버 값을 그대로 쓰고 `reports`로 재계산하지 않음. 남은 결함: 보고가 없는 행(`workReportId: null`)은 배지가 다른 심사대기 행과 같아 보이지만 클릭 불가(`/task-reports/null`로 갈 수 없어서) — 미해결로 남김. `reports[].status` 허용값 표가 명세에 없어 `PENDING`은 같은 응답의 `progress.pending`을 근거로 처리하기로 하고 백엔드 질문 5번에 기록.
- **알려진 퇴행 1건**: 업무 상세에서 제출된 보고를 누르면 이제 실제 `workReportId`로 이동하는데, 업무보고 화면(`/task-reports`)은 여전히 mock이라 그 id를 못 찾아 "찾을 수 없습니다"가 뜬다. 더미 제거의 대가이며 업무보고 API 명세가 나와야 풀림.
- **`| tail -25`로 e2e 결과를 자른 것 때문에 "374 passed"로 잘못 보고**: 파이프 이후 관찰한 종료 코드는 `tail`의 것이라 실제 실행 결과(83~84건 실패)를 놓쳤다. 원인·재발 방지는 [[쉘-파이프-tail-종료코드-오판]] 참고.
- **e2e 전체 실패 83~84건을 세 원인으로 분류**(이 브랜치의 작업과 무관, develop에서도 동일하게 실패 확인): ① 공지·자료실·휴관일·운영안내 퍼블리싱 스펙에 `page.route()` mock이 0개라 실서버를 호출(~59~60건) ② close-dat 계열 fixture 날짜가 고정 과거값이라 "이번 달" 필터에서 사라짐(~20건, [[테스트-픽스처-고정날짜-현재월-필터-노후화]]과 동일 계열) ③ develop이 이번에 도입한 401·403 전역 로그아웃(`configureApiAuthentication`, 재발급 실패 시 401·즉시 403 모두 `/login` 이동)이 "오류 화면 유지"를 기대하는 승인 시나리오와 어긋남(~12건, 업무지시 10건 포함). CI가 없어(워크플로 0개, 필수 status check 없음, 머지 조건은 리뷰 승인 1명) 이 실패들이 PR을 막지 않는다는 점을 확인 후, 세 원인을 이슈 3개로 분리해 별도 처리하기로 결정(같은 PR에 섞으면 리뷰 대상이 흐려짐).
- **develop 124커밋 머지**(`git merge origin/develop`, 리베이스 대신 — 이미 push된 34커밋을 다시 얹지 않으려는 목적, 저장소 전체에 같은 형태의 develop→피처 머지가 7번 있어 팀 관례로 확인). 충돌 9개: `App.tsx`(develop의 `RequireAuth`/`AppLayout` 구조에 `/tasks/:id/edit` 라우트만 추가), `DataTable.tsx`(우리 표 외형 옵션 위에 develop의 열별 `align`·`paddingX`·클릭 불가 행 커서를 다시 얹음, 두 정렬 개념이 같은 자리를 다퉈 `resolveAlign()`으로 병합), `task-{create,edit,list}.spec.ts` 토큰 값, 승인 해시 파일 4개(`*.approved.json`, 손으로 못 합쳐 `approve.mjs` 재발급). develop이 dev 프록시를 제거하고 `VITE_API_BASE_URL` 절대 URL을 강제하도록 바뀌어 업무지시 e2e mock의 route 패턴(`/api/...`)도 절대 URL 패턴으로 일괄 치환 필요했음. 해소 검증은 "충돌 파일을 고쳤다"는 주장이 아니라 **양쪽 기능이 실제로 동작하는지**로 확인 — develop의 work-log 스펙 56개가 전부 통과한 것으로 develop이 다시 얹은 `DataTable` 기능이 살아있음을 확인.
- **PR 본문 관례**: 이 저장소 PR 템플릿(`📌 관련 이슈`/`✨ 작업 내용`/`🛠️ 변경 사항`/`💬 리뷰어에게`) 기준, 리뷰어에게 항목엔 "지금 실패 상태인 것"과 "그 이유가 이 PR 범위 밖"이라는 근거를 함께 적어 리뷰어가 실패를 이 PR 탓으로 오인하지 않게 하는 패턴을 씀.

## 출처
- Claude Code 세션 자동 캡처 (/data/project/ToyVillage-Admin-FE) — 2026-09-07 오후(목록 퍼블리싱 착수), 2026-09-07 저녁(목록 완료·push, 생성/수정/상세 spec 재작성), 2026-09-08(생성·수정·상세 퍼블리싱 완료·커밋, 레이아웃 버그 수정, 상태 모델 변경), 2026-09-09(업무지시 API 연동 spec·Contract 작성, develop merge), 2026-09-10(업무지시 API 연동 승인·구현·검증 완료, close-dat 시간 의존 테스트 실패 원인 진단), 2026-09-11(배럴 import 근본 원인 확정·수정, 전체 스펙 재검증, task-report S16 원인 확정, freeze 8건, 백엔드 미배포 실토큰 확인, 커밋 12개 분리), 2026-09-12(목록 assignees 스키마 수정, 케밥 메뉴 클리핑 수정, 업무보고·진행도 연동, develop 124커밋 머지·충돌 해소, e2e 83건 원인 3분류, 커밋 3개+머지 커밋, push, PR 본문 작성)
