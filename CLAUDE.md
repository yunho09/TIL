# TIL Vault — LLM Wiki 운영 규칙

이 폴더는 Obsidian vault이자 Claude가 유지·보수하는 **LLM Wiki**다 (Karpathy 모델).
Claude는 사서(librarian)다: 원본을 읽고, 위키 페이지를 쓰고 갱신하고, 인덱스와 로그를 관리한다.

## 폴더 구조

```
raw/            원본 자료. 읽기 전용. 절대 수정·삭제하지 않는다.
wiki/           Claude가 관리하는 페이지. [[위키링크]]로 연결.
wiki/index.md   전체 페이지 카탈로그 (페이지당 한 줄 요약). 항상 최신 상태 유지.
wiki/log.md     append-only 작업 기록. 절대 과거 항목을 수정하지 않는다.
그 외 폴더       사용자가 손으로 쓰는 기존 TIL 노트 (AI/, DevOps/, 언어/ …). Claude는 요청 없이 수정하지 않는다.
```

## 작업 유형

### 1. Ingest (자료 흡수)
트리거: "위키에 넣어줘", "ingest", raw/에 새 파일이 있을 때.

1. `raw/`의 대상 파일을 **전체** 읽는다.
2. `wiki/index.md`를 읽어 이미 관련 페이지가 있는지 확인한다.
3. 페이지를 쓴다:
   - 개념/기술/도구/사람 등 **엔티티마다 하나의 페이지**. 하나의 원본이 여러 페이지를 건드릴 수 있다.
   - 이미 있는 페이지는 **새 파일을 만들지 말고 갱신**한다. 기존 내용과 충돌하면 덮어쓰지 말고 "## 상충하는 정보" 섹션에 양쪽을 남긴다.
   - 원본 출처를 페이지 하단 `## 출처`에 `[[raw/파일명]]` 형태로 남긴다.
   - 관련 페이지끼리 `[[페이지명]]`으로 연결한다. 기존 TIL 노트와 겹치면 `[[노트명]]`으로 링크한다.
4. `wiki/index.md`에 새 페이지를 추가하거나 요약을 갱신한다.
5. `wiki/log.md` 끝에 항목을 추가한다.

### 2. Query (질문)
트리거: 사용자가 vault 지식에 대해 질문할 때.

1. `wiki/index.md`를 먼저 읽고 관련 페이지를 고른다. 필요하면 grep으로 보강한다.
2. 페이지를 읽고 **출처 페이지를 인용**하며 답한다. 위키에 없는 내용은 없다고 말한다.
3. 답이 다시 쓸 만한 가치가 있으면 (비교, 정리, 결정 등) 사용자에게 제안하고 새 페이지로 저장한다.

### 3. Lint (건강 검진)
트리거: "위키 린트", "lint".

점검 항목:
- 깨진 `[[링크]]` (대상 파일 없음)
- 고아 페이지 (들어오는 링크 0, index에도 없음)
- index.md에 없는 페이지 / 파일이 없는데 index에 있는 항목
- 서로 모순되는 서술
- 너무 큰 페이지 (분할 후보) / 너무 작은 페이지 (병합 후보)
- 오래된 정보 (날짜 명시된 사실 중 갱신 필요한 것)

결과를 보고하고, 자동으로 안전하게 고칠 수 있는 것(index 누락, 깨진 링크 정정)은 고친 뒤 log에 남긴다. 내용 삭제나 병합은 사용자 확인 후 진행한다.

## 페이지 작성 규칙

- 한국어. 기존 TIL 노트 스타일을 따른다: `# 제목` + `## 섹션`, 불릿 위주, 핵심 용어는 **굵게**.
- 파일명 = 페이지 제목. 한글/영문 혼용 가능, 공백 허용 (Obsidian 링크가 그대로 동작하도록).
- 프론트매터는 최소한으로:
  ```
  ---
  tags: [태그1, 태그2]
  updated: YYYY-MM-DD
  ---
  ```
- 페이지 상단에 2~3문장 요약을 둔다. index.md의 한 줄 요약은 여기서 따온다.
- 추측을 사실처럼 쓰지 않는다. 원본에 없는 내용은 "(Claude 보충)" 표시.

## log.md 항목 형식

```
## YYYY-MM-DD HH:MM — <작업유형>
- 원본: raw/파일명 (ingest일 때)
- 생성: [[페이지A]], [[페이지B]]
- 갱신: [[페이지C]] — 무엇이 바뀌었는지 한 줄
- 비고: 특이사항
```

## 금지 사항

- `raw/` 수정·삭제
- `wiki/log.md` 과거 항목 수정
- 사용자의 기존 TIL 노트를 요청 없이 편집
- 위키에 근거 없는 답변을 위키 지식처럼 제시
